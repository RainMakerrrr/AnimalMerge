# AnimalMerge — Knowledge Base
> Загружается автоматически при старте каждой сессии. Обновляй после значимой работы.

## Проект
Turn-based mobile battler с механикой merge. Unity 2022.3.62f3 · C# · Zenject · UniTask · DOTween · TextMeshPro.
Платформы: Android & iOS. Основная ветка разработки: `iteration_2/Alexandr/Develop`.

## Архитектура
- **Clean Architecture**: Presentation → Application → Domain → Infrastructure
- **DI**: Zenject — никаких Singleton
- **Async**: UniTask везде — никаких Coroutines в новом коде
- **Паттерны**: State (BattleStateMachine), Command (Merge undo), Factory, Facade (AnimalFacade), Observer (SignalBus), Strategy (ITutorialStep, IMergeHintTargetResolver, IBattleReadinessRule)
- **Детали**: `AgentsDocs/ProjectArchitecture.md`

## Карта модулей
| Модуль | Путь | Статус |
|---|---|---|
| Battle State Machine | Code/Battle/StateMachine/ | Активен |
| Очередь ходов | Code/Battle/Services/ (`TurnOrderCalculator`, `TurnOrderProvider`) · Code/Battle/UI/ | Активен — снапшот раунда пишет `TurnExecutor` через `ITurnOrderRecorder`, колонка аватарок читает через `ITurnOrderProvider` |
| Pre-Battle фаза | Code/Battle/PreBattle/ · Config/ · UI/ · Signals/ | Активен — спавн союзников, readiness-правила; пулами владеет `PreBattleConfig` (`StartingPool` ур. 1 + `RandomPool` ур. 2+), `AllySpawnPool` — «тупая» очередь |
| Tutorial | Code/Tutorial/ | Активен — каркас шагов `ITutorialStep`, merge-hint рука, обвязка сцены закоммичена |
| GridPathfinding (A*) | Code/GridPathfinding/ | Активен — multi-size units; **одна сетка `GameGrid` 8×12** (`MergeGrid` удалена 2026-08-17), зона деплоя = 2 нижние строки (`DeploymentZone`) |
| Animal System | Code/Animals/ | Активен — в игре все 6 player-префабов; пулы живут в `PreBattleConfig.asset`, а НЕ в `AnimalSpawner`; `ChickenFlock` — футпринт стаи 2х2 × 4 |
| Враги | Resources/Prefabs/Enemies/ · Code/Animals/Facades/ | Активен — 6 префабов: `EnemyCheetah/Chicken/Elephant_Temp`, `T-Rex`, `Velociraptor`, `Pteranodon` (подключён к `LevelStageConfig_2.asset`) |
| Выбор животного | Code/Animals/Selection/ · Code/Animals/UI/ | Активен — `IAnimalSelectionService` (владеет подсветкой хода, гейт по пре-батл фазе), карточка статов `AnimalStatsPanel{View,Presenter}` |
| Шейдеры / VFX | Assets/Shaders/ · Code/Animals/Materials/ · Code/Animals/Vfx/ | Активен — IridescentSweep (loot shine + дух смерти + merge-свип), SpiritGhost, GridCellBorder, система merge-анимаций (`MergeAnimationConfig`) |
| Ability System | Code/Abilities/ | Активен (Dodge, CounterAttack, Retreat) — все value-agnostic, шансы приходят из подклассов `AnimalStats` |
| Data / ScriptableObjects | Code/Data/ | Активен — per-animal тюнинг в подклассах `AnimalStats` (`FoxStats`, `HedgehogStats`) |
| Framework (bootstrap + верхнеуровневый FSM) | Code/Framework/ | Активен — переработан 2026-08-01: флоу победы/поражения, `CampaignVictoryState`, сервисы Progression/GameRestart, окна результата |
| Сброс боя | Code/Battle/Services/ | Активен — `IBattleResetService` (уничтожение юнитов + `BattleFlowController.Cleanup()`) |
| Камера боя | Code/Battle/CameraControl/ | Активен — зум-ин при старте боя, зум-аут при возврате к подготовке и в конце боя; **долли по `transform.forward`, а НЕ анимация FOV** (дочерняя `HealthBarCamera` за FOV родителя не следует); параметры — `Settings/BattleConfigs/BattleCameraConfig.asset` |
| Боевой HUD | Code/Battle/UI/ · Code/Animals/UI/ · Framework/Code/UI/Elements/ · Resources/Canvas.prefab | Активен — **итерация 1 завершена целиком 2026-09-08** (код + вёрстка): контейнер `HUD` с `SafeArea` в `Canvas.prefab`, LEVEL / RESTART / UNDO MERGE / ADD ANIMAL / FIGHT, карточка статов + `EnemyCardView` (карточка врага от `IUnitTracker`, НЕ от выбора), счётчик пула на ADD ANIMAL, grayscale-состояние FIGHT, штриховка и иконка врага на `HealthBar.prefab`. `CanvasScaler` 1080×1920 **`match = 0` (Width)** — иное значение сужает канвас на экранах выше 16:9 |
| Editor-тулзы | Code/Editor/ | Активен — `AnimalPrefabBuilder` (валидатор + генератор префабов), `AnimalPortraits` (запекание портретов), `BattleSceneBuilder` (сборка боевой сцены в Edit Mode), `GridManagerSetup`, `HealthBarSetupTool`; весь код целиком под `#if UNITY_EDITOR` — отдельного Editor-asmdef в проекте нет |
| Pathfinding/, NewPathfinding/ | Code/Pathfinding/ | DEPRECATED — удалить позже |

## Важные файлы
- `AgentsDocs/ProjectArchitecture.md` — полная документация модулей
- `AgentsDocs/ZenjectPatterns.md` — паттерны DI и SignalBus
- `AgentsDocs/Battle_State_System_Setup_Guide.md` — setup battle системы
- `AgentsDocs/Specifications/` — спецификации систем (`README.md` — индекс; 01–05 по pathfinding/merge/attack/timings, `06_Balance_Sync_With_Concept_Doc.md` — сверка баланса с концепт-доком 3.0 (**числа применены и проверены на диске 2026-08-02, §8**; открыты: §2.5 «футпринт птеродактиля 2х1» — блокер код (`UnitSize {2,1}` не поддерживается стеком сетки), §2.8 «иглы ежа» — блокер арт, §3 «прогрессия» — блокер геймдизайн), `2026-06-14-agent-pipeline-design.md`)

## 📗 Для геймдизайнера
**`Knowledge/GameDesignerGuide.md`** — где что настраивается (баланс, уровни, пулы, сетки, тексты,
анимации мерджа, туториал), какие правила ломают игру молча, что сейчас сломано, и чем реализация
расходится с концепт-доком. Обновляй при изменении любого настраиваемого ассета.

## 🎯 Источник истины по геймдизайну и балансу
**ClickUp, концепт-док 3.0, `q8n65-4592`** — https://app.clickup.com/24401093/v/dc/q8n65-4592/q8n65-3432
Числа в проекте обязаны соответствовать ему; при расхождении «код vs док» побеждает док.
Страницы: `q8n65-3432` Концепт-док 3.0 (корневая — дублирует обе таблицы + механики) · `q8n65-3472` Overview · `q8n65-3492` Gameplay · `q8n65-3552` Merge & Combat · `q8n65-3672` **Abilities** · `q8n65-3572` **Heroes & Bosses** (таблицы животных и боссов) · `q8n65-3592` Progression & Economy · `q8n65-3612` Levels · `q8n65-3632` UI/UX (+ дети ART, VFX) · `q8n65-3652` Ideas & Questions.
⚠️ **Док местами противоречит сам себе по процентам скиллов** (таблица животных vs страница «Abilities»). Универсального правила «страница X главнее» НЕТ — разбирать по одному, спрашивая владельца. HP / дамаг / скорость / размер — из таблиц.
- **Додж лисы 50/30** — осознанный нерф (80/50 было имбалансным), а не победа страницы над страницей. Таблица в ClickUp приведена к 50/30 на обеих страницах 2026-08-02, расхождения больше нет.
- **Контратака ежа 50/50** — вот здесь именно противоречие (таблица даёт владельцу 100%, проза и «Abilities» — 50%), владелец выбрал 50/50. **Таблица в ClickUp НЕ правлена — расхождение «док vs код» по ежу открыто.**
⚠️ **Правка страниц ClickUp через API — только полной заменой контента, и она нормализует разметку** (снимает жирность в шапках таблиц, поднимает вложенные абзацы в пункты списка, схлопывает пустые строки). Ради одной ячейки несоразмерно — быстрее и безопаснее руками в UI.
- `AgentsDocs/CodeStyle.md` — соглашения по коду
- `.claude/CLAUDE.md` — инструкции для агента

## Последние изменения
_Обновляй при каждой сессии._

- [2026-09-08] **Боевой HUD по макету — ИТЕРАЦИЯ 1 ЗАВЕРШЕНА ЦЕЛИКОМ: доделана часть 2, редакторская.** ✅ Play Mode рабочий, фича отсмотрена вживую — прежняя запись «Play Mode падает без `EnemyCardView.prefab`» **недействительна**. Собран HUD в `Assets/Resources/Canvas.prefab` + `Assets/Scenes/Main Scene.unity`: контейнер `HUD` с `SafeArea`, плашка LEVEL (левый верх), RESTART (правый верх), UNDO MERGE (левый низ), ADD ANIMAL (низ центр), FIGHT (правый низ); доведены `AnimalStatsPanelView.prefab` (аватар с кольцом, статы, статические строки перков), новый `Assets/Prefabs/EnemyCardView.prefab` и `HealthBar.prefab` (штриховка + иконка врага); `Taptic Button`, `Coins Count`, `Heal All Units` и дубль `Restart` **скрыты (`SetActive false`), а не удалены** — их нет на макете. 🔴 **Девять ловушек Unity, и все молчат — ни ошибки, ни варнинга**: (1) `CanvasScaler.match` обязан быть **0 (Width)**, а не 1 и не 0.5 — `scaleFactor = (sw/1080)^(1-match)·(sh/1920)^match`, поэтому на любом экране выше 16:9 канвас в reference-единицах **уже** 1080 (при 0.5 это 978 юнитов на 1080×2340 / 1170×2532 / 1284×2778, нижний ряд перекрывался на ~41 px мёртвой зоной рейкаста); (2) нижний ряд интринсически тесный — `110 + 462 + 352 + поля = 964` из 1080, на два зазора 116, **ADD ANIMAL центрировать невозможно** (`x = -121`, зазоры по 58), и несущее здесь то, что **`UiPulseAnimator` растит FIGHT на 8% ВЛЕВО от пивота (1,0) в активном состоянии** — при `-73` перекрытие 18 юнитов даже на идеальном 1080, то есть баг вылезал бы **только когда кнопка загорается**; (3) **TMP игнорирует `Graphic.material` молча** (роутит через `TMP_MaterialManager`) — материал только через `TMP_Text.fontMaterial`, в `BattleButtonView` выбрано гасить лейблы цветом: `AcceptsMaterial(graphic) => (graphic is TMP_Text) == false`; (4) **двойное затемнение**: если disabled-цвет красит код, у `Button` обязан стоять `transition = None`, иначе `Selectable` домножает `ColorTint` сверху — наступили дважды (FIGHT и ADD ANIMAL, получалось `~{0.16, 0.22, 0.09}`, почти чёрный вместо приглушённого лайма); (5) `_notReadyColor` **считается, а не угадывается** — `UIGrayscale` при `_Saturation = 0` даёт `lerp(luminance, rgb, _Saturation)`, светимость оранжевого тела `button_2` = 0.57, поэтому 0.55 сделал бы надпись невидимой, итог **0.72**; (6) штриховка на Filled-заливке = **`Mask` на `Fill` + `Fill/FillHatch` Tiled-оверлеем внутри**: `Fill` обязан остаться `m_Type: 3` (иначе умирает `fillAmount`), Filled **не тайлит** (тайл 32×32 растягивается на ~100×26, диагональ 45° сплющивается до ~8°), а обычный ребёнок Filled-картинки **не обрезается** по `fillAmount` и красит пустую часть бара; (7) любой элемент фиксированного размера в HP-баре обязан делиться на `ownerScale` вслед за `HealthBarView.ApplyFootprintSize()` (бар держит постоянный **мировой** размер, масштабы животных от **0.35** у ежа до **1.0** у курицы) — отсюда сериализованный `_enemyIconSize`; (8) **`SafeArea` двигает не весь UI** — `Level Counter` внутри `Canvas/HUD`, а `TurnOrderView` **прямой ребёнок `Canvas`**: при вырезе плашка едет вниз, колонка стоит, наезд на 1170×2532 **не виден без симуляции safe area** (`TurnOrderView.prefab` сдвинут на `m_AnchoredPosition (36, -320)`); (9) для `button_2.png` **9-slice геометрически невозможен** — измеренные скосы требуют border `top ≥ 65` и `bottom ≥ 68`, суммарно 133 > 130 px высоты, плюс спрайт несёт **два отдельных декоративных штриха** (есть и на макете), которые 9-slice растягивает в мусор → `Image.type = Simple`, `spriteBorder {0,0,0,0}`, **арт не править** (попытка стереть штрихи откачена, PNG байт-в-байт как в `afaefa1e`). 🔴 **Подтверждено на живом Play Mode**: все шесть врагов стадии 2 репортят `ChickenFacade`, наследующийся от `PlayerAnimalFacade` — **сторону юнита по типу фасада определять нельзя**, только через `IUnitTracker`; для этого в `IUnitTracker`/`UnitTracker` заведено событие **`EnemyUnitRegistered`** (реактивного аналога для вражеской стороны не было, `PlayerUnitsChanged` существовал только для союзников). Карточка врага не уворачивается от якоря — `protected virtual bool ShouldAvoidCoveringAnchor` с оверрайдом `false` в `EnemyCardView`, `ClampInsideCanvas` работает в обеих ветках. ✅ **Проверка**: целевые наборы EditMode `AllySpawnPoolTests` / `AllySpawnServiceTests` / `BattleReadinessServiceTests` — **20/20**, PlayMode `BattleButtonViewTests` / `AddAnimalButtonViewTests` — **7/7**. 🔴 **Предсуществующие падения, к задаче не относятся, ничего не отключалось и не чинилось**: 4 в `HealthBarViewTests` (проверено откатом `HealthBarView.cs` на HEAD — те же 4), плюс `AnimalAttackPostAbilityTests`, `RetreatAbilityTests`, `TargetPositionCalculatorTests`. ⚠️ **Инструментальное**: мост к Unity в обход упавшего MCP-клиента — `mcp-for-unity-server` на `http://127.0.0.1:8080/mcp` по HTTP JSON-RPC (`initialize` → `Mcp-Session-Id` → `notifications/initialized` → `tools/call`), все 48 инструментов через `curl`; `execute_code` требует `"action":"execute"` и **обязательный `return` в конце**, компилируется как **C# 6**; у `run_tests` фильтрация через `group_names`, не `test_filter`; `manage_script` без `validate`; **прогон PlayMode-тестов через MCP выставляет `m_EnterPlayModeOptionsEnabled: 1`** — возвращать через `UnityEditor.EditorSettings.enterPlayModeOptionsEnabled = false` + `AssetDatabase.SaveAssets()`, а не правкой файла. 🔴 **Коммитить только вместе с камерой боя** (`EnemyCardPresenter` подписан на `BattleEndedSignal`, `BattleInstaller.cs` несёт правки обеих задач). Детали и все выкладки — `Decisions/2026-09-08-battle-hud-layout-editor-part.md`
- [2026-09-07] **Боевой HUD по макету — ИТЕРАЦИЯ 1, ЧАСТЬ 1: только C# и плейсхолдер-арт.** ✅ **[поправка 2026-09-08: фича ЗАВЕРШЕНА ЦЕЛИКОМ, часть 2 сделана — см. запись от 2026-09-08 выше; всё «не сделано» и «Play Mode падает» ниже недействительно]** На момент записи 2026-09-07 — редакторская часть (создание `EnemyCardView.prefab`, раскладка в `Canvas.prefab`, назначение сериализованных ссылок и спрайтов, 9-slice, `CanvasScaler.match` 1 → 0.5, `Button.transition` у UNDO, скрытие дубля `Restart`) не сделана: Unity MCP всю сессию — `ConnectionRefused`. 🔴 **Play Mode сейчас падает, и это ожидаемо**: `BindEnemyCard()` — `NonLazy` + `FromComponentInNewPrefab(_enemyCardPrefab)`, а `Assets/Prefabs/EnemyCardView.prefab` ещё не создан и в `Main Scene` не назначен. Сделано в коде: счётчик ADD ANIMAL «03 / 05» (`IAllySpawnPool.Total` — инкремент в `Enqueue`, сброс в `Clear`; `IAllySpawnService.PoolTotal`; `AddAnimalButtonView.SetRemaining(remaining, total)`, два лейбла, формат `D2`; знаменатель = сколько всего положили в пул за фазу — решение владельца, не `MinAlliesToStart`); disabled-вид FIGHT **без нового арта** — `BattleButtonView` получил `Graphic[] _stateGraphics` + `Material _notReadyMaterial` (существующий `UI_Sprites/UIGrayscale.mat`), авторские материалы кэшируются лениво в `Dictionary<Graphic, Material>` и **восстанавливаются, а не затираются в `null`**, а тинт `_notReadyColor` НЕ домножается поверх grayscale (двойное затемнение); UNDO MERGE — **кода не потребовалось вообще** (`interactable` уже есть, disabled-вид даёт `Button.transition = ColorTint`); `IAnimalStatsPanelView.Show()` расширен на `title` и `IReadOnlyList<string> abilityLines` с трёхзначным контрактом (`null` = «не трогать лейблы, оставить статический текст префаба» — выражено константой `KeepPrefabAbilityLines`; пустой список = «способностей нет, погасить»; непустой = данные), **оба презентера пока передают `null`**, потому что `IAbility` не отдаёт ни имени, ни шанса; карточка врага — `EnemyCardView : AnimalStatsPanelView, IEnemyCardView` (пустой сабкласс, чтобы не заводить обратную зависимость `Code.Animals.UI` → `Code.Battle.UI`) + `EnemyCardPresenter` + `BindEnemyCard` в `BattleInstaller`; `LevelView` пишет только число; новый `SafeArea.cs` (**safe area в проекте не обрабатывалась НИГДЕ**); плейсхолдеры `UI_Sprites/Placeholder/` (5 png); импорт `button_1/2.png` починен с `Default`+`None` (такую текстуру **нельзя назначить в `Image`**) на `Sprite (2D and UI)` / `Single` / `Full Rect` / 9-slice. 🔴 **Три факта разведки дороже самой вёрстки**: (1) `AnimalStatsPanelView` — **screen-space, а НЕ world-space** (один экземпляр в корне overlay-`Canvas`, проекция `WorldToScreenPoint` в `LateUpdate`; единственный настоящий world-space UI — `HealthBar.prefab` на слое 8 с отдельной `HealthBarCamera`); (2) **врага нельзя выбрать тапом** — `AnimalMover.CastRay()` кастует маску `Animal`, враги на слое `Enemy` (23), поэтому карточка врага не может ехать через `IAnimalSelectionService` и получила свой презентер от `IUnitTracker` + сигналов боя; (3) **TMP молча игнорирует `Graphic.material`** — материальные эффекты на `TextMeshProUGUI` ставятся только через `TMP_Text.fontMaterial`, иначе присваивание проходит без ошибки и без эффекта. Проверка: компиляция — локальный .NET SDK по Unity `csproj` (`CodeBase`, `Code.Editor.Tests`, `Code.PlayMode.Tests` — 0 ошибок, 5 дозадачных варнингов); консоль Unity и тесты НЕ смотрелись. Детали, контракт `abilityLines` и полный чек-лист части 2 — `Decisions/2026-09-07-battle-hud-layout-iteration-1.md`
- [2026-09-07] **Приближение камеры при старте боя — модуль `Assets/Code/Battle/CameraControl/`** (`IBattleCameraService` / `BattleCameraService` / `BattleCameraPresenter`, 6 новых `.cs` + ассет `Settings/BattleConfigs/BattleCameraConfig.asset`, параметры наезда/отъезда в руках дизайнера: `_dollyDistance` `[Range(0, 4.5)]` = 3.5, длительности 0.6 / 0.45, две `AnimationCurve`, `_zoomInDelay`). 🔴 **Зум сделан долли — сдвигом Transform вдоль `forward`, а НЕ анимацией FOV**: у `Main Camera` есть дочерняя `HealthBarCamera` (clearFlags Depth only, culling mask = слой 8 `HealthBar`, свой FOV 60), и анимация `Camera.main.fieldOfView` рассинхронизировала бы **все** хелсбары с юнитами — дочерняя камера сохранила бы свой FOV; долли двигает родительский Transform, дочерняя едет следом по построению. **Триггер — не клик по Fight** (`BattleReadinessService` может отклонить старт, камера уехала бы впустую), а новый `BattleStartedSignal` из `PreBattleState.StartBattleAsync()`; отъезд слушает `BattleEndedSignal` (из `BattleEndState`, после гварда death-animation и до `CleanupLevel()`) **и** существующий `PreBattlePhaseStartedSignal` — второй обязателен, потому что сцена не перезагружается и переходы между стадиями / рестарт иначе оставили бы камеру в наезде. Твин — ручной UniTask-цикл с `AnimationCurve` (DOTween непригоден: `UNITASK_DOTWEEN_SUPPORT` не определён, `DOTWEEN_ASYNC` — только под Android → await'абельный твин непортируем на iOS); каждый твин стартует от ТЕКУЩЕЙ позиции + внутренний CTS отменяет предыдущий, поэтому прерывание даёт плавный доворот, а не скачок. ⚠️ **Не импортировано Unity** (MCP всю сессию — ConnectionRefused, `.meta`/`.asset` YAML писались руками, компиляция проверена msbuild'ом по копии `CodeBase.csproj`). Детали — `Decisions/2026-09-07-battle-camera-zoom.md`
- [2026-08-17] **`MergeGrid` удалена — одна `GameGrid` 8×12, зона деплоя стала логическим понятием.** Отдельная мерж-сетка 8×2 в `(0,0,−3)` стёрта целиком (код, сцена, `MergeGridConfig.asset`); `GameGridConfig._height` 10 → 12, transform `GameGrid` z: 0 → −2, враги во всех 4 `LevelStageConfig` переехали с y = 9 на y = 11 (13 записей). **Мир визуально не изменился** (враги на прежних мировых координатах, z = 9.5), а зона деплоя сдвинулась на клетку ближе к полю — дыра между сетками исчезла; логическая дистанция «союзник → враг» осознанно выросла на 2 клетки (медленным юнитам с `TilesPerMove` 1–3 нужен ~+1 ход). Новый `DeploymentZone` (`Depth = 2`, `Contains`, `ContainsAll`) заменил магическую `1` в четырёх местах `GridCell` и в `GridConfig.MinHeight`; `GridIdentifier` удалён, все 9 `[Inject(Id = ...)]` разыменованы, `PathfindingInstaller` держит один `Bind<IGridManager>().FromInstance(_gameGridManager).AsSingle()` (попутно исправлен `.AsTransient()` на `FromInstance`). 🔴 **Границу зоны теперь держит логика, а не геометрия** — раньше выход за строки 0–1 предотвращала сама высота `MergeGrid`, а `CanPlaceUnit` выход за границы ловить НЕ умеет (`GetOccupiedCellsInternal` молча выбрасывает `null`-клетки), поэтому на сетке 8×12 футпринты 1×2 и 2×2 с якорем y = 1 молча заняли бы боевую строку 2 без единой ошибки в консоли; добавлен гейт `FitsInDeploymentZone` в `TryFindFreePlacement`, где **проверка «количество клеток == площадь футпринта» несущая**. 🔴 **`ChickenMergeSkill.FindFreeMergeCell` — та же ловушка, найдена только на ревью**: искал клетку под клона по всей сетке и был безопасен ровно потому, что сетка была 8×2; починен через `IsFreeDeploymentPlacement` + делегирование фолбэка в `TryFindFreePlacement` (поднят в `IGridManager`). Структурно закрылись телепорт союзника на первом ходу и костыль `MoveRangeHighlighter.IsOnGameGrid` — гейт подсветки убран совсем, радиус хода показывается и на стартовых позициях (возврат к исходному замыслу от 2026-06-27). Детали — `Decisions/2026-08-17-single-game-grid-deployment-zone.md`
- [2026-08-13] **Battle Scene Builder — Editor-инструмент сборки боевой сцены** (`Assets/Code/Editor/BattleSceneBuilder/`, 15 новых файлов, namespace `Code.Editor.BattleSceneBuilder`, каждый целиком под `#if UNITY_EDITOR`; рантайм-код не менялся ни в одном файле). Пункт меню `Tools/AnimalMerge/Scene Builder`: выбираешь `LevelStageConfig` + `PreBattleConfig`, кнопка Build спавнит в Edit Mode обе сетки, союзников стартового пула на `MergeGrid` и врагов стадии на `GameGrid`, кнопка Clear убирает всё, не трогая то, что стояло в сцене до запуска. 🔴 **Ключевой приём — засев приватного `GridManager._cells` рефлексией**: `EnsureGridInitialized()` рано выходит при `!Application.isPlaying`, поэтому в Edit Mode `CanPlaceUnit` итерирует ПУСТОЙ список клеток и возвращает `true` для ЛЮБОЙ позиции → `TryFindFreePlacement` всегда отдаёт `(0,0)` → все союзники молча схлопываются в один угол, без единой ошибки в консоли; после засева работают настоящие `TryFindFreePlacement`/`CanPlaceUnit`/`GetOccupiedCells`/`SetOccupied`, и логика раскладки не дублируется. Всё созданное живёт под root `[AnimalMerge] SceneBuilder` с маркер-компонентом (поиск обходом `GetRootGameObjects()` → переживает domain reload), `Build` идёт одной collapsed Undo-группой, `BattleSceneBuilderSceneGuard` снимает превью на `ExitingEditMode` и на `EditorSceneManager.sceneSaving`. ⚠️ ~~**Файлы не импортированы редактором — `.meta` не сгенерированы**~~ (**поправка 2026-08-17: неверно, файлы в git с `74fcf89d` вместе с `.meta`**); компиляция на момент записи проверялась в обход редактора: временный csproj + msbuild с определённым `UNITY_EDITOR`, 0 ошибок. **[2026-08-17] Союзники теперь идут не на `MergeGrid`, а в зону деплоя единой `GameGrid`.** Детали — `Decisions/2026-08-13-battle-scene-builder.md`
- [2026-08-02] **Очередность ходов — доработка: плавный транзишн + два CRITICAL от ревью.** Убитый враг залипал в колонке, потому что `UnitTracker.OnUnitRemoved` фаерит `PlayerUnitsChanged` только при `removedFromPlayers`, а на `Health.Died` трекер подписан лишь для союзников — смерть врага не вызывала НИКАКОГО обновления UI; починено новым `UnitTurnCompletedSignal { AnimalFacade Unit }` (фаерится в `TurnExecutor.ExecuteUnitTurnsAsync` после `await ExecuteSingleUnitTurnAsync`, внутри foreach после guard'а — Started/Completed всегда парные; `IUnitTracker` НЕ трогали). Плавная ротация: **`VerticalLayoutGroup` + `ContentSizeFitter` УДАЛЕНЫ из `TurnOrderView.prefab`** (лэйаут-группа не умеет анимировать перестановку) — слоты считает код (`_entryHeight`/`_entrySpacing`), энтри едут твином `anchoredPosition`, высоту колонки выставляет сам `Render`; идентичность энтри — `TurnOrderEntryData.Id = unit.GetInstanceID()` (`Render` матчит существующие вью по `Id`, и ротация становится движением, а не перестройкой); кроссфейд серый↔цветной — два наложенных `Image` (нижний `Portrait` с ОБЩИМ `UIGrayscale.mat`, верхний `PortraitColor` без материала, твинится альфа верхнего). 🔴 **Два CRITICAL от ревью**: (1) fallback-проход `TakeAvailableEntry` исключал только `_next`, но не `_active` — пул отдавал ЖИВУЮ вью другого юнита, `Render` считал её `animated: true`, пропускал `Restore()` и анимировал чужую аватарку (каждый ход, когда юнитов больше `_maxEntries`: уровень 1 — 9 юнитов при 6 слотах), fallback удалён, пул стабилизируется на `_maxEntries + 1`; (2) сторона юнита определялась по типу фасада (`unit is EnemyAnimalFacade`), а все три temp-врага (`EnemyChicken/Cheetah/Elephant_Temp`) наследуются от `PlayerAnimalFacade` — ходящий враг получал СОЮЗНИЧЕСКУЮ оранжевую рамку (6 из 11 юнитов на уровне 1), починено новым `TurnOrderUnit { AnimalFacade Unit; bool IsEnemy }` — сторона проставляется из того списка трекера, откуда юнит пришёл. Детали — `Decisions/2026-08-02-turn-order-column.md`
- [2026-08-02] **Очередность ходов — колонка аватарок** (левый верхний угол под кнопкой вибрации, порядок сверху вниз, ходящий цветной/увеличенный/в рамке и ротируется наверх, остальные серые). Данных об очереди в проекте не существовало: порядок считался инлайн дважды внутри `TurnExecutor` и выбрасывался. Вынесен в `TurnOrderCalculator` (LINQ-цепочки перенесены вербатим), рядом встали `ITurnOrderProvider` / `ITurnOrderRecorder` (read/write разделены, обе реализует `TurnOrderProvider`, `BindInterfacesTo<TurnOrderProvider>().AsSingle()`) и сигналы `UnitTurnStartedSignal` / `TurnRoundStartedSignal` / `UnitTurnCompletedSignal` (`.OptionalSubscriber()`). 🔴 **Провайдер отдаёт СНАПШОТ раунда, который executor реально итерирует, а не живой пересчёт** — юниты движутся во время своего хода, ключи сортировки меняются посреди раунда, и уже походивший юнит пересортировывался бы выше ещё не походивших. UI: `TurnOrderView` (пул entry, `_maxEntries` 6, `CanvasGroup`-fade, репарент под `Canvas` в `Awake`, sibling index 0) + `TurnOrderEntryView` + `TurnOrderPresenter` + DTO `TurnOrderEntryData`. Существующие `UI_Sprites/AnimalHeads/*.png` оказались непригодны (все 9 — чисто белые силуэты, форма только в альфе, grayscale от белого = белый) — написана editor-тула `AnimalPortraitBaker` (`Tools/Animals/Bake Turn Order Portraits`), запёкшая 9 цветных портретов из 3D-префабов в `Assets/UI_Sprites/AnimalPortraits/`; `AnimalConfig` получил ОТДЕЛЬНОЕ поле `Portrait` (`GetPortrait()` = `Portrait ?? Icon`), попутно починен `Icon: {fileID: 0}` у `Pterodactyl`. Обесцвечивание — `Shaders/UIGrayscale.shader` + один общий материал. `Canvas.prefab` не трогали. Детали — `Decisions/2026-08-02-turn-order-column.md`
- [2026-08-02] **Валидатор префабов животных — Фаза 1 тулзы генерации** (`Assets/Code/Editor/AnimalPrefabBuilder/`, 9 файлов, namespace `Code.Editor.AnimalPrefabBuilder`, каждый целиком в `#if UNITY_EDITOR` — `CodeBase.asmdef` имеет `includePlatforms: []`, поэтому папка Editor уезжает в плеерную сборку). Пункты меню `Tools/Animals/Validate Animal Prefabs` и `.../Validate Selected Animal Prefab`. 20 правил кодируют ловушки, оплаченные ручной сборкой птеранодона: неподдерживаемый `UnitSize`, отсутствие события `AttackAnimationHandler`, `IsDead`-как-Trigger, отсутствие Any State → Death, слой дочернего коллайдера, маска атаки, авторинг `MergeScaleAnimator`, расхождение `GetFootprint`, дубли `_type`, покрытие merge-сферой луча `TryPlace`, полнота `_colliders`. **Аудит 12 префабов: 19 ошибок, 31 предупреждение. Единственный чистый префаб — `Pteranodon`** (собран вручную вчера по этим же правилам). **Фаза 2 — генератор**: рецепты-SO (`AllyAnimalRecipe` / `EnemyAnimalRecipe`, ассеты в `Assets/Settings/Animals/Recipes/`), вывод геометрии из `Renderer.bounds` + футпринта, сборка плоского префаба, мердж-обвязка с визуал-слотами по кости, `AnimalDatabaseWriter` (load-or-create), генерация фасада врага. Приёмка: птеранодон собран заново — **22 из 25 полей совпали с ручным, валидатор даёт 0 находок**. Ключевые находки и опровергнутая формула радиуса в `Decisions/2026-08-02-animal-prefab-validator.md`
- [2026-08-02] **Баланс пересверен с концепт-доком и применён заново — правки 2026-08-01 до диска НЕ доехали.** Прямое чтение `Assets/Settings/Animals/Stats/*.asset` показало, что не сохранилась НИ ОДНА из семи правок §2.1–2.5 (ёж скорость оставалась 3, курица 100/2, птеродактиль 200/3); совпадали только изначально верные Слон/Олень/Гепард/T-Rex/Велоцираптор. Переприменено правкой YAML + `AssetDatabase.ImportAsset(ForceUpdate)` и проверено и на диске, и через `SerializedObject`: `HedgehogStats._tilesPerMove 3→1` и `_ownerCounterChance 100→50`, `ChickenStats._health 100→25` / `_tilesPerMove 2→3`, `PterodactylStats._health 200→350` / `_tilesPerMove 3→5`. **Два процента скиллов уточнены решением владельца** (док противоречит сам себе): додж лисы — **50/30**, а не 80/50 (правка спеки 06 §2.2 была ошибкой, ассет всё это время был прав; откачены только константы `FoxStats.cs` 80/50→50/30); контратака ежа — **50/50**, а не 100/50 (`HedgehogStats.cs` `DefaultOwnerCounterChance` 100→50). Числовой баланс всех девяти реализованных юнитов теперь совпадает с доком. Спека 06 переписана: добавлен §8 с полной сверкой, §7 помечен как неверная запись
- [2026-08-01] **Враг Pteranodon собран в префаб** `Resources/Prefabs/Enemies/Pteranodon.prefab` из объекта сцены: канонический набор компонентов врага по образцу `T-Rex`/`Velociraptor` (`Animator`, `PterodactylFacade`, `AnimalMovement`, `UnitOccupancy`, `AnimalAttack`, `AnimalHealth`, `AnimalAnimator`, `DamagePopupController` + дети `Collider` на слое 23, `AttackPoint`, вложенный `HealthBar`), новый `PterodactylFacade : EnemyAnimalFacade`, клипы `Pteranodon_Attack.anim` (с обязательным событием `AttackAnimationHandler`) и синтезированный `Pteranodon_Death.anim`, переписанный `Pterodactyl.controller` с **Any State → Death по `IsDead == true`**, офсет 1.3 в `HealthBarSetupTool`. Футпринт **2х1 из спеки 06 §2.5 реализован и откачен к 1х1** — `{2,1}` не поддерживается стеком сетки и делает врага молча неиграбельным
- [2026-08-01] **Баланс синхронизирован с концепт-доком 3.0** (спека 06, §2.1–2.7 и §2.9): ⚠️ **числовые правки в статах до диска НЕ доехали — переприменены 2026-08-02, см. запись выше**; стая курицы стала **ровно 4 юнита блоком 2х2** — новый `ChickenFlock` (`Footprint`/`Count`), `GridManager.TryFindFreePlacement` как единственный алгоритм поиска места (`HasCellFor` и `PlaceOnGrid` делегируют, попутно починен латентный boundary-баг), `IAnimalFactory.SpawnAdditionalChickens` → атомарный `CreateChickenFlock(IReadOnlyList<IGridCell>)`, `AnimalSpawner.Spawn` ветвится до инстанцирования (нет блока 2х2 → нет ни одной курицы); клон и оригинал курицы визуально ×0.75 (`MergeAnimationConfig._cloneScaleMultiplier`, `MergeScaleAnimator.ScaleBy`/`UndoScaleBy` + новый `Resync`); шанс контратаки вынесен в новый `HedgehogStats` (100/50), `CounterAttack` стал value-agnostic. Попутно из `AnimalFactory` удалён мёртвый DI (`SetMergeGrid`, `_mergeGrid`, `_gridManager`)
- [2026-08-01] **Карточка статов животного по тапу + подсветка хода на выборе**: новый слой `Code/Animals/Selection/` (`IAnimalSelectionService`/`AnimalSelectionService`) стал единственным источником истины о выбранном животном — забрал `IMoveRangeHighlighter` у `AnimalMover`, сам гейтит выбор по `PreBattlePhaseStarted/Ended` и самозалечивается по fake-null в `Tick()` (несколько путей уничтожения не зовут `NotifyRemoved()`). `AnimalMover` стал чистым переводчиком жеста (тап выбирает, повторный тап и тап по пустому снимают, драг сбрасывает, выбор переживает отпускание), `IsPointerOverUi()` починен под тач (`fingerId`). Карточка — `Code/Animals/UI/AnimalStatsPanel{View,Presenter}` + `Assets/Prefabs/AnimalStatsPanelView.prefab`, читает рантайм-значения `AnimalFacade.GetDamage()`/`GetCurrentHealth()`, а не `AnimalStats`. `AnimalConfig` получил `Icon` + `GetIcon()`, иконки для всех 9 типов нарисованы процедурно через `execute_code` в `Assets/UI_Sprites/`; кнопки Add/Battle перекрашены в цвета макета
- [2026-08-01] ~~**Пулы спавна сведены в `AnimalSpawner`**~~ (**откачено в `088b406f`: оба пула снова в `PreBattleConfig`**) **+ подключены Deer и Hedgehog**: два массива `_startingPool` (ур. 1) / `_randomPool` (ур. 2+, все 6 player-префабов), `PreBattleConfig` лишился `StartingPool` и оставил только `MinAlliesToStart`/`ReinforcementsPerLevel`, `AllySpawnPool.RefillFromConfig()` заменён на `IAllySpawnService.QueueStartingPool()`. `Deer.prefab` получил `DeerFacade`/`MergeTarget`/`AttackPoint` (без фасада он не попадал в `AnimalFactory` и падал с `KeyNotFoundException`), починены `_animator`/`_attackPoint`/`_mask` (была 0 — олень не видел цели), заведены `Deer_Attack_Horns_1.anim` с анимационным событием (без него первый удар вешал ход навсегда) и `Deer_Death.anim` + `Any State → DDeath`; `HedgehogFacade._mergeView` подключён, радиусы merge-сфер обоих пересчитаны под `AnimalMovement.TryPlace()`. Удалён мёртвый `AutoFight.cs` и bookkeeping `IAnimalSpawner.Animals`; `AnimalFactory.Create` больше не бросает исключение
- [2026-08-01] **Флоу победы/поражения переработан**: после последнего уровня — экран финальной Victory (`CampaignVictoryState` → «ANIMALS WON» + кнопка RESTART, полный рестарт кампании), Lose-панель получила кнопку TRY AGAIN вместо мгновенного авто-рестарта. Новые `ICampaignProgressService`, `IGameRestartService`, `IBattleResetService` (`Code/Battle/Services/`), `WindowType.CampaignVictory`, `DefeatWindowView`/`CampaignVictoryWindowView` + `GameResultPresenter`. `LevelFactory` больше не заворачивает индекс уровня по `%` (кампания молча зацикливалась), `CollectablesData.ResetAmount()` разделён на `RevertLevelAmount()`/`Clear()`, `GameStateMachine` шлёт `StateChangedSignal` до `Enter()`, `RestartButton` переведён на сервис. Обвязка `Canvas.prefab` и `Main Scene.unity` выполнена
- [2026-08-01] **Общая система анимаций при мердже**: новые типы в `Code/Animals/Vfx/` — `MergeAnimationConfig` (ScriptableObject со всеми тюнингами, ассет в `Resources/`), `MergeScaleTween`, `RendererMaterialOverlay`, `MergeAppearAnimation`, `MergeScaleAnimator`. Слон растёт скейл-анимацией с перелётом, слот-детали (хвост/рога — везде, где есть `MergeVisualSlot`) появляются со scale-in параллельно с glow-пассом, клон курицы получает ту же appear-анимацию, окрас гепарда визуально не изменился. Побочно починены три предсуществующих бага: undo слона в цепочке A→B→C, взаимное затирание двух `RendererMaterialOverlay` на одном рендерере, `Deer.prefab` `SlotMergeAttribute._type: 0 → 2` (рога не появлялись никогда)
- [2026-08-01] **Туториальная рука для мерджа + каркас туториалов**: новый модуль `Code/Tutorial/` — `TutorialRunner` идёт по `List<ITutorialStep>`, шаг сам ждёт свой триггер внутри `RunAsync` и возвращает `bool`; `MergeHintStep` показывает `TutorialHandView` (DOTween-слайд с одного животного на другое) до первого `AllyMergedSignal`, пара берётся по индексу спавна из `IUnitTracker`; прогресс — PlayerPrefs, ключ `Tutorial.{stepId}`; конфиг `Settings/TutorialConfigs/MergeHintStepConfig.asset` (уровень 1, режим `SpawnOrder`, индексы 0 → 1)
- [2026-08-01] **Glow-свип при мердже Cheetah**: волна света проезжает по всему животному → под ней накладывается материал гепарда → волна уезжает обратно → эффект убирается полностью. `IridescentSweep.shader` получил явный world-режим развёртки (`_SweepWorldAxis.w` = флаг, `_SweepWorldOrigin`), новый драйвер `Code/Animals/Vfx/MergeSweepGlow.cs` (обычный C#-класс, не MonoBehaviour) гонит фазу на `UniTask.Yield`, `CheetahMergeAttribute` переписан с сохранением синхронных `Apply`/`Undo` через `.Forget()`
- [2026-07-31] **Смерть курицы** (союзной и вражеской): анимация падения уже была нарисована, но не подключена (`IsDead` вёл в `jump`) — доразведены существующие стейты `down`/`wing_down` через `Any State`, `IsDead` стал Bool; плюс новый эффект вылета духа — `Code/Animals/Vfx/` (`DeathSpiritSpawner`/`DeathSpiritView`), дух = запечённая через `BakeMesh` копия меша курицы с материалом `GhostSpirit.mat`. `EnemyChicken_Temp.prefab` делит тот же контроллер, поэтому получил разводку автоматически — доставлен только компонент
- [2026-07-31] Эффект **«loot shine»** — переливающаяся волна света по объекту: шейдер `Assets/Shaders/IridescentSweep.shader` (Built-in RP, unlit аддитивный оверлей **вторым слотом материала**, анимация целиком на `_Time.y`, ни строки C#), материал `Code/Animals/Materials/IridescentSweepOverlay.mat` повешен на `Fox Tail` в `Resources/Prefabs/Animals/Cheetah.prefab`
- [2026-07-31] Массовый спавн союзников заменён на кнопки **AddAnimal** (одно животное за клик) и **Battle** с пульсацией; логика спавна вынесена из UI в прикладной слой — новые модули `Code/Battle/PreBattle/`, `Code/Battle/UI/`, `Code/Battle/Signals/`, конфиг `PreBattleConfig`
- [2026-07-30] Размеры игровых полей (`MergeGrid` 8×2 / `GameGrid` 8×10) вынесены из хардкода `GridManager` в ScriptableObject-конфиги `GridConfig` (`Code/GridPathfinding/Config/`, ассеты в `Settings/GridConfigs/`) — `IGridManager` не менялся
- [2026-06-27] Вероятности Dodge для Fox вынесены из хардкода в данные — подкласс `FoxStats : AnimalStats` (Owner 50% / Inherited 30%, `[Range(0,100)]`), ассет `FoxStats.asset` сконвертирован на новый тип; `Dodge` теперь принимает `int successChance` вместо `bool isOwner`
- [2026-06-27] Текстовая анимация (popup) при кросс-тайп мердже животных — `MergePopupView`/`MergePopupController` (Code/Animals/UI/), событие `MergeTarget.Merged`, строки per-animal в `AnimalConfig.MergeInfo`
- [2026-06-27] Подсветка доступных ходов животного при удержании клика/тапа (BFS-радиус по TilesPerMove) — новая сущность `MoveRangeHighlighter`
- [2026-05-27] Setup Codex, обновлены настройки проекта
- [2026-05-27] Добавлено рисование gizmos для AOE атаки
- [2026-05-27] Реализован cheetah texture changer (нужны доработки)
- [2026-05-27] Удалён кастомный MCP сервер, установлен официальный Unity MCP

## Текущее состояние
_Что сейчас в работе._

Ветка `iteration_2/Alexandr/Develop` активна. Последняя задача — **боевой HUD по макету, итерация 1** (`Code/Battle/UI/`, `Code/Animals/UI/`, `Framework/Code/UI/Elements/` + `Canvas.prefab` / `Main Scene`): ✅ **[2026-09-08] завершена целиком — и код, и редакторская часть**, Play Mode рабочий, фича отсмотрена вживую (прежние записи «часть 2 не сделана» и «Play Mode сломан» недействительны); в рабочем дереве, не закоммичена. Параллельно другим агентом сделано **приближение камеры при старте боя** (`Code/Battle/CameraControl/` + `BattleCameraConfig`; в рабочем дереве, не закоммичена, **и не импортирована редактором**). ✅ **Поправка: удаление `MergeGrid` уехало в коммит `4efd3ae1`** — прежняя запись «в рабочем дереве, не закоммичено» недействительна; Battle Scene Builder закоммичен в `74fcf89d` вместе с `.meta`. Очередность ходов вместе с плавным транзишном и двумя фиксами от ревью уехала в `35a3a029` + `33f3e0a8`; тулза генерации префабов животных, пересверка баланса и вражеский префаб Pteranodon — в `c26564b4`…`0bd898fa`. Три ранее незакоммиченные фичи (баланс/стая курицы, пулы спавна + паритет префабов Deer/Hedgehog, карточка статов) уехали в `088b406f` и `534c11a1`; поверх них лёг `519685df` (health bars). Флоу победы/поражения и система merge-анимаций — в `ce7b3485`/`51763a96`; merge-свип гепарда и туториальная рука — в `9f7ed831`/`fd66f1d9`.

### Боевой HUD по макету — `Code/Battle/UI/` · `Code/Animals/UI/` · `Framework/Code/UI/Elements/` + `Canvas.prefab` / `Main Scene` (в рабочем дереве, не закоммичено, ✅ **ФИЧА ЗАВЕРШЕНА ЦЕЛИКОМ**)
- ✅ **[2026-09-08] Итерация 1 завершена полностью — и код, и редакторская часть.** Прежние записи «часть 2 не сделана» и «Play Mode сломан» **недействительны**: `Assets/Prefabs/EnemyCardView.prefab` создан и назначен, `NonLazy`-биндинг `BindEnemyCard()` резолвится, фича отсмотрена в живом Play Mode.
- **Собранный HUD** — `Assets/Resources/Canvas.prefab` + `Assets/Scenes/Main Scene.unity`: контейнер `HUD` с компонентом `SafeArea`, плашка LEVEL (левый верх), RESTART (правый верх), UNDO MERGE (левый низ), ADD ANIMAL (низ центр), FIGHT (правый низ). Доведены `AnimalStatsPanelView.prefab` (аватар с кольцом, статы, статические строки перков), новый `EnemyCardView.prefab` и `HealthBar.prefab` (штриховка + иконка врага). `Taptic Button`, `Coins Count`, `Heal All Units` и дубль `Restart` **скрыты (`SetActive false`), а не удалены** — их нет на макете.
- 🔴 **`CanvasScaler.match` = 0 (Width), а не 0.5 и не 1.** `scaleFactor = (sw/1080)^(1-match)·(sh/1920)^match`, поэтому на любом экране выше 16:9 канвас в reference-единицах **уже** 1080: при `0.5` — 978 юнитов на 1080×2340 / 1170×2532 / 1284×2778, и нижний ряд HUD перекрывался на ~41 px мёртвой зоной рейкаста. При `0` канвас всегда ровно 1080 в ширину, меняется только высота.
- 🔴 **Нижний ряд интринсически тесный, ADD ANIMAL центрировать невозможно.** `110 + 462 + 352 + поля = 964` из 1080, на два зазора остаётся 116 юнитов; итог — `anchoredPosition.x = -121`, зазоры по 58. Несущая деталь: **`UiPulseAnimator` растит FIGHT на 8% ВЛЕВО от пивота (1,0) в активном состоянии** — при `-73` это давало реальное перекрытие 18 юнитов даже на идеальном 1080, то есть баг проявлялся бы **только когда кнопка загорается**.
- 🔴 **TMP игнорирует `Graphic.material` молча.** Материальные эффекты (grayscale и т.п.) на `TextMeshProUGUI` через `Graphic.material` **не работают** — без ошибок, TMP роутит через `TMP_MaterialManager`; ставить только через `TMP_Text.fontMaterial` либо гасить лейблы цветом. В `BattleButtonView` выбрано второе: `AcceptsMaterial(graphic) => (graphic is TMP_Text) == false`.
- 🔴 **Двойное затемнение disabled-состояния.** Если код сам красит графику в disabled-цвет, у `Button` обязан стоять `transition = None` — иначе `Selectable` накладывает `ColorTint` сверху и цвета **перемножаются**. Наступили дважды (FIGHT и ADD ANIMAL): получалось `~{0.16, 0.22, 0.09}`, почти чёрный вместо приглушённого лайма.
- **`_notReadyColor` = 0.72 — посчитан, а не подобран.** Шейдер `UIGrayscale` при `_Saturation = 0` даёт `lerp(luminance, rgb, _Saturation)`, светимость оранжевого тела `button_2` = **0.57**, поэтому «на глаз» взятые 0.55 сделали бы надпись невидимой.
- 🔴 **Штриховка HP-бара = `Mask` на `Fill` + `Fill/FillHatch` как Tiled-оверлей внутри.** `Fill` обязан остаться `m_Type: 3` (Filled), иначе перестаёт работать `fillAmount`, а Filled **не тайлит** (тайл 32×32 растягивается на ~100×26, диагональ 45° сплющивается до ~8°); простой дочерний оверлей тоже не годится — ребёнок Filled-картинки **не обрезается** по `fillAmount` и красит пустую часть бара. Filled генерирует меш только на заполненную долю, стенсил режет детей ровно по краю заливки.
- 🔴 **Всё фиксированного размера в HP-баре делится на `ownerScale`.** `HealthBarView.ApplyFootprintSize()` делит `sizeDelta` на `movement.transform.localScale`, чтобы бар держал постоянный **мировой** размер; без той же компенсации добавленный элемент отличается кратно масштабу животного (в проекте от **0.35** у ежа до **1.0** у курицы). Отсюда сериализованный `_enemyIconSize`.
- 🔴 **`AnimalStatsPanelView` — screen-space, а НЕ world-space** (единственный экземпляр в корне overlay-`Canvas`, проекция `Camera.WorldToScreenPoint` в `LateUpdate`); единственный настоящий world-space UI в префабах животных — `HealthBar.prefab`. Логика `ResolvePanelPosition` / `CoversAnchor` уводит панель от юнита, карточке врага это не нужно — добавлен `protected virtual bool ShouldAvoidCoveringAnchor` с оверрайдом `false` в `EnemyCardView`; `ClampInsideCanvas` работает в обеих ветках.
- 🔴 **`SafeArea` двигает не весь UI.** `Level Counter` лежит внутри `Canvas/HUD` с `SafeArea`, а `TurnOrderView` — **прямой ребёнок `Canvas`**: при вырезе плашка едет вниз, колонка стоит, отсюда наезд на 1170×2532, которого **не видно без симуляции safe area**. `TurnOrderView.prefab` сдвинут на `m_AnchoredPosition (36, -320)`.
- 🔴 **Сторона юнита — только из `IUnitTracker`, подтверждено на живом Play Mode**: все шесть врагов стадии 2 репортят `ChickenFacade`, который наследуется от `PlayerAnimalFacade`. Врага при этом **нельзя выбрать тапом** (`AnimalMover.CastRay()` кастует `LayerMask.GetMask("Animal")`, враги на слое `Enemy` 23), поэтому карточка врага не едет через `IAnimalSelectionService`, а имеет свой `EnemyCardPresenter`. В `IUnitTracker`/`UnitTracker` добавлено событие **`EnemyUnitRegistered`** — реактивного аналога для вражеской стороны не существовало (`PlayerUnitsChanged` был только для союзников).
- 🔴 **`button_2.png`: `Image.type = Simple`, `spriteBorder {0,0,0,0}` — 9-slice геометрически невозможен.** Измеренные скосы требуют вертикальных border `top ≥ 65` и `bottom ≥ 68`, суммарно 133 > 130 px высоты спрайта. Плюс спрайт **содержит два отдельных декоративных штриха**, не связанных с телом восьмиугольника (вверху слева, внизу справа) — они есть и на макете, при 9-slice растягиваются в мусор. **Арт не править**: первая попытка стереть штрихи была ошибкой и откачена, PNG байт-в-байт как в `afaefa1e`.
- **Счётчик ADD ANIMAL «03 / 05»**: `IAllySpawnPool.Total` (инкремент в `Enqueue`, сброс в `Clear`) → `IAllySpawnService.PoolTotal` → `AddAnimalButtonView.SetRemaining(int remaining, int total)`; два раздельных лейбла, формат `D2`. Знаменатель = сколько всего положили в пул за фазу (решение владельца), а НЕ `MinAlliesToStart`.
- **Карточка статов — контракт `abilityLines`.** `Show(anchor, icon, attack, health, string title, IReadOnlyList<string> abilityLines)`; `null` — «не трогать лейблы, оставить статический текст префаба» (константа `KeepPrefabAbilityLines`), пустой список — «погасить», непустой — рисовать. **Оба презентера пока передают `null`**: `IAbility` не отдаёт ни имени, ни шанса, поэтому строки перков — статический текст префаба.
- **UNDO MERGE — кода не написано ни строки**: `interactable = stackCount > 0` уже был, disabled-вид даёт штатный `Button.transition`.
- ⚠️ **Мост к Unity в обход упавшего MCP-клиента** (пригодится снова): сервер `mcp-for-unity-server` на `http://127.0.0.1:8080/mcp`, HTTP JSON-RPC (`initialize` → `Mcp-Session-Id` → `notifications/initialized` → `tools/call`), все 48 инструментов доступны через `curl`. Оговорки: `execute_code` требует `"action":"execute"` и **обязательный `return` в конце**, компилируется как **C# 6**; у `run_tests` фильтрация через `group_names`, а не `test_filter`; `manage_script` в этой сборке без `validate`. 🔴 **Прогон PlayMode-тестов через MCP выставляет `ProjectSettings/EditorSettings.asset → m_EnterPlayModeOptionsEnabled: 1`** — возвращать через `UnityEditor.EditorSettings.enterPlayModeOptionsEnabled = false` + `AssetDatabase.SaveAssets()`, а **не правкой файла** (Unity перезапишет из памяти).
- ✅ **Проверка**: живой Play Mode отсмотрен; целевые наборы — EditMode `AllySpawnPoolTests` / `AllySpawnServiceTests` / `BattleReadinessServiceTests` **20/20**, PlayMode `BattleButtonViewTests` / `AddAnimalButtonViewTests` **7/7**. 🔴 **Предсуществующие падения, к задаче не относятся и не чинились**: 4 в `HealthBarViewTests` (проверено откатом `HealthBarView.cs` на HEAD — те же 4), плюс `AnimalAttackPostAbilityTests`, `RetreatAbilityTests`, `TargetPositionCalculatorTests`. Действует **TESTS PAUSED**.
- ⚠️ **Весь арт в `Assets/UI_Sprites/Placeholder/` временный** (7 файлов): `icon_lightning`, `icon_boss_skull`, `icon_undo`, `avatar_ring`, `hpbar_hatch`, `icon_attack_paw_white`, `icon_health_heart_white`.
- **Открытые вопросы**: «Kickback» в коде отсутствует (по числам похоже на `CounterAttack`, у ежа 50%) — строки перков остаются статическими, реальные проценты требуют расширения `IAbility` (вторая итерация); иконка на HP-баре показывается **всем врагам, а не только боссам** (переход на boss-only — правка одного условия, `AnimalFacade.IsBoss` / `StageEnemyConfig.IsBoss` есть); из UI пропал тумблер вибрации (`Taptic Button` скрыт); `TurnOrderView` на макете отсутствует, но оставлен как живая функциональность — только сдвинут.
- Часть 1 (C#-слой, контракты) — `Decisions/2026-09-07-battle-hud-layout-iteration-1.md`; часть 2 (редакторская, все ловушки выше) — `Decisions/2026-09-08-battle-hud-layout-editor-part.md`

### Камера боя — `Code/Battle/CameraControl/` (в рабочем дереве, не закоммичено, НЕ импортировано Unity)
- **Что делает**: камера наезжает на поле, когда бой реально начался, и отъезжает обратно при возврате к подготовке и в конце боя. Три класса: `IBattleCameraService` (`ZoomInAsync` / `ZoomOutAsync` с `CancellationToken`), `BattleCameraService` (считает базу и цель один раз в конструкторе, крутит твин, владеет отменой), `BattleCameraPresenter` (`IInitializable` + `IDisposable`, подписки + `_lifetimeCts`, вызовы через `.SuppressCancellationThrow().Forget()`).
- 🔴 **Зум = долли, а НЕ FOV — и это не стилистический выбор.** У `Main Camera` есть дочерняя `HealthBarCamera` (clearFlags Depth only, culling mask = слой 8 `HealthBar`, собственный FOV 60): хелсбары рисуются второй камерой поверх мира и совпадают с юнитами ровно потому, что обе камеры смотрят одинаково. Анимация `Camera.main.fieldOfView` увела бы родителя и оставила ребёнка — полоски поехали бы относительно голов на всё время наезда. `BattleCameraService` двигает `transform.position` вдоль `transform.forward` на `DollyDistance`, дочерняя камера едет следом по построению и о ней никто не знает. **Если когда-нибудь понадобится именно FOV — гнать его по всем `GetComponentsInChildren<Camera>()`.**
- 🔴 **Триггер наезда — `PreBattleState.StartBattleAsync()`, а не обработчик клика по Fight.** `BattleReadinessService` может отклонить запрос (минимум союзников, пул), и по клику камера уезжала бы в бой, которого не будет. `BattleStartedSignal` фаерится перед `ChangeStateAsync<PlayerTurnState>()` — это единственная точка «бой прошёл все проверки и начинается».
- 🔴 **Отъезд слушает ДВА сигнала, и второй обязателен.** `BattleEndedSignal` (новый, фаерится в `BattleEndState.Enter()` после гварда отмены death-animation и до `CleanupLevel()`) покрывает конец боя; существующий `PreBattlePhaseStartedSignal` покрывает переход между стадиями и рестарт уровня — **сцена не перезагружается**, эти пути не проходят через `BattleEndState`, и без второй подписки камера осталась бы в наезде.
- **Твин — ручной UniTask-цикл на `UniTask.Yield(PlayerLoopTiming.Update)` с `Vector3.LerpUnclamped` и `AnimationCurve`, не DOTween.** `UNITASK_DOTWEEN_SUPPORT` в проекте не определён, `DOTWEEN_ASYNC` определён **только для Android** → await'абельный DOTween-твин непортируем на iOS. Образец, с которого списано, — `Assets/Code/Animals/Vfx/MergeScaleTween.cs`.
- **Прерывание отработано**: каждый твин стартует от ТЕКУЩЕЙ позиции камеры (не от номинальной базы/цели) и первым делом гасит предыдущий через внутренний CTS (`CreateLinkedTokenSource` с внешним токеном). Зум-аут посреди зум-ина даёт плавный доворот, а не скачок; повторный зум-аут идемпотентен (base → base, завершается за кадр).
- **Namespace `Code.Battle.CameraControl`, а не `Code.Battle.Camera`** — во втором случае идентификатор `Camera` резолвится в namespace вместо `UnityEngine.Camera` и всё падает на CS0118. Общее правило для любого модуля, названного именем типа Unity.
- **Конфиг** `Assets/Settings/BattleConfigs/BattleCameraConfig.asset` (`Game/Battle Camera Config`): `_dollyDistance` `[Range(0, 4.5)]` = 3.5, `_zoomInDuration` = 0.6, `_zoomInCurve` (EaseInOut), `_zoomInDelay` = 0, `_zoomOutDuration` = 0.45, `_zoomOutCurve` (EaseInOut). ⚠️ **Верхняя граница 4.5 привязана к позе камеры в сцене** (pos `(3.06, 16.09, −8.76)`, pitch 45°, FOV 60): при d ≈ 4.73 из кадра уходит задний ряд зоны деплоя (z = −2.5). Подвинут камеру — пересчитать `Range`.
- **DI**: `BattleInstaller` получил `[SerializeField] _battleCameraConfig` (ассет назначен в `Main Scene`, дифф сцены — одна строка), два `DeclareSignal<...>().OptionalSubscriber()` и `BindBattleCamera()` (`FromInstance` конфига + `BindInterfacesTo<BattleCameraService>()` + `BindInterfacesAndSelfTo<BattleCameraPresenter>().NonLazy()`), плюс проверку в `ValidateSceneReferences()`. Отдельный биндинг камеры не понадобился — `CurrentGameInstaller.BindCamera()` уже держит `Bind<Camera>().FromInstance(Camera.main).AsSingle()`. `BattleStateMachine` прокидывает `signalBus` в конструктор `BattleEndState`.
- ⚠️ **Проверка неполная**: Unity MCP всю сессию отдавал ConnectionRefused, поэтому все `.meta` и `.asset` YAML написаны руками, а компиляция проверена в обход редактора — msbuild по сгенерированной копии `CodeBase.csproj` (чисто, два преэкзистующих варнинга). **Asset refresh, консоль и Play Mode — не пройдены.**
- Детали и рассмотренные альтернативы — `Decisions/2026-09-07-battle-camera-zoom.md`

### Единая сетка и зона деплоя — `Code/GridPathfinding/` (закоммичено, `4efd3ae1`)
- **`MergeGrid` больше не существует.** Была отдельная сетка 8×2 в `(0,0,−3)` со своим `GridManager`, своим `GridConfig` и своим Zenject-биндингом. Удалена целиком: из кода, из `Main Scene`, ассет `MergeGridConfig.asset` стёрт. Теперь одна `GameGrid` **8×12** в `(0,0,−2)`; животные создаются и переносятся только в двух нижних строках (зона деплоя).
- **Геометрия — Вариант A (решение владельца).** `GameGridConfig._height` 10 → 12, transform z: 0 → −2, враги во всех 4 `LevelStageConfig` с y = 9 на y = 11 (13 записей). **Мир визуально не изменился** — боевое поле и враги на прежних мировых координатах (враги z = 9.5); зона деплоя сдвинулась на клетку ближе к полю (была z ∈ [−3,−1], стала z ∈ [−2,0]), дыра между сетками исчезла. ⚠️ **Логическая дистанция «союзник → враг» осознанно выросла на 2 клетки** — медленным юнитам (`TilesPerMove` 1–3: ёж, гепард, олень) нужен примерно **+1 ход** на сближение. Альтернатива (оставить врагов на y = 9) сохраняла баланс байт-в-байт, но двигала всю картинку на 2 юнита к камере — отклонена.
- **`DeploymentZone`** (`Assets/Code/GridPathfinding/DeploymentZone.cs`, статический): `Depth = 2`, `Contains(int y)`, `ContainsAll(IReadOnlyList<IGridCell>)`. Заменил магическую `1` в четырёх местах `GridCell` (`CanPlace`, `IsInDeploymentZone`, два сравнения в `IsPositionValidForPlacement`) и `GridConfig.MinHeight`.
- **DI**: `GridIdentifier` удалён, все 9 сайтов `[Inject(Id = ...)]` разыменованы. `PathfindingInstaller` держит один `Container.Bind<IGridManager>().FromInstance(_gameGridManager).AsSingle()` — попутно исправлен `.AsTransient()` на `FromInstance` (на инстансе ничего не давал, кроме ложного сигнала о времени жизни).
- 🔴 **Границу зоны теперь держит логика, а не геометрия — главная ловушка задачи.** Раньше выход юнита за строки 0–1 предотвращала сама высота `MergeGrid` (ровно 2): `AdjustPositionToFitBounds` сдвигал якорь обратно. **`CanPlaceUnit` выход за границы ловить НЕ умеет** — `GetOccupiedCellsInternal` молча выбрасывает `null`-клетки, проверка идёт только по найденным. На сетке высотой 12 футпринты 1×2 (Cheetah/Deer/Fox/Hedgehog) и 2×2 (Elephant, стая куриц) с якорем y = 1 молча заняли бы боевую строку 2 — **без единой ошибки в консоли**. Отсюда гейт `FitsInDeploymentZone` в `GridManager.TryFindFreePlacement`, где **проверка `footprintCells.Count == size.Width * size.Height` несущая, а не защитная**: без неё свисающий за границу футпринт проходит именно потому, что недостающие клетки отброшены как `null`, и `ContainsAll` подтверждает оставшиеся. Мёртвый `GetSortedFreeCells` удалён.
- 🔴 **`ChickenMergeSkill.FindFreeMergeCell` — та же ловушка, найдена только на ревью.** Метод искал клетку под клона «по всей сетке», и это было безопасно **ровно потому**, что сетка была 8×2. На 8×12 мердж в строке 1 ронял клона в строку 2, а column-major фолбэк предпочитал клетку `(0,5)` в поле свободной `(1,0)` в зоне. Починено: adjacent-probe гейтится новым `IsFreeDeploymentPlacement` (bounds → `DeploymentZone` → `CanPlaceUnit` → полнота футпринта), фолбэк делегирован в зоно-осведомлённый `TryFindFreePlacement`, ради чего он поднят в интерфейс `IGridManager`. **Урок шире задачи: при снятии неявного ограничения искать надо не только код, который его проверял, но и код, который на нём молча ехал.**
- **Структурно закрылось три бага**: союзник на `MergeGrid` логически алиасился на `GameGrid(x, 0/1)` (координаты совпадали, `GridCell.Equals` сравнивает по X/Y) — отсюда телепорт на первом ходу, костыль `MoveRangeHighlighter.IsOnGameGrid` (`ReferenceEquals` ради отличения одноимённых клеток) и поиск места под клона курицы на мерж-сетке даже для юнита в середине поля.
- **`MoveRangeHighlighter` — гейт удалён, радиус хода подсвечивается и на стартовых позициях.** Сначала костыль `IsOnGameGrid` был заменён явным `if (DeploymentZone.Contains(unit.CurrentPathNode.Y)) return;` (владелец выбрал «сохранить поведение»), но при проверке в Play Mode выяснилось, что вопрос был понят иначе — подсветка нужна везде, и гейт убран совсем. Это **возврат к исходному замыслу**: [[2026-06-27-move-range-highlighter]] фиксировал «радиус хода по ВСЕЙ сетке (без ограничения зоной деплоя Y<=1) — это превью боевого перемещения, а не зона размещения», а `IsOnGameGrid` был артефактом двух сеток. Ограничение «первые две клетки» относится к ручному размещению (`GridCell.Accept`), а не к ходу в бою.
- **Проверка**: ёмкость зоны на 8×12 проверена экспериментально — 1×1 → 16 юнитов, 1×2 → 8, 2×2 → 4, ни один футпринт не залез в строку 2; **ёмкость идентична старой `MergeGrid` 8×2**. `GridConfigTests` 5/5 и `GridConfigRuntimeTests` 6/6 зелёные. Новых тестов нет (**TESTS PAUSED**).
- ❌ **Отклонено на ревью и откачено**: в диффе оказался `ProjectSettings/EditorSettings.asset` с отключёнными domain reload и scene reload — project-wide настройка, меняющая время жизни статики между Play-сессиями, к задаче отношения не имеет.
- Детали и рассмотренные альтернативы — `Decisions/2026-08-17-single-game-grid-deployment-zone.md`

### Battle Scene Builder — `Assets/Code/Editor/BattleSceneBuilder/` (закоммичено, `74fcf89d`; переведён на единую сетку 2026-08-17)
- **Что делает**: `Tools/AnimalMerge/Scene Builder` → ObjectField для `LevelStageConfig` и для `PreBattleConfig`, сворачиваемый блок Scene References с `GridManager` + кнопкой Resolve, тумблеры Build Allies / Build Enemies, кнопки Build и Clear, HelpBox-предупреждения, отчёт последнего запуска. ⚠️ **[2026-08-17] Сетка одна**: и союзники стартового пула, и враги стадии идут на `GameGrid` — союзники в зону деплоя (строки 0–1), враги в строку 11. Прежняя запись «союзники на `MergeGrid`» недействительна. Порядок Build: `Clear()` → сетки → враги → союзники, всё в одной collapsed Undo-группе.
- 🔴 **Засев приватного `GridManager._cells` рефлексией — ядро инструмента, и без него он молча ВРЁТ, а не падает.** `EnsureGridInitialized()` (`GridManager.cs:84`) делает ранний выход при `!Application.isPlaying`, поэтому в Edit Mode `_cells == null` и `GetCell(...)` всегда `null`. Дальше по цепочке: `CanPlaceUnit` (`:303`) итерирует пустой список клеток и возвращает `true` для ЛЮБОЙ позиции → `TryFindFreePlacement` (`:699`) отдаёт `(0,0)` на первой же итерации → все союзники схлопываются в один угол. Ошибок в консоли при этом ноль. После засева настоящие `TryFindFreePlacement`/`CanPlaceUnit`/`GetOccupiedCells`/`SetOccupied`/`HasCellFor` работают в Edit Mode как в рантайме, и логика раскладки не дублируется. Рефлексия, а не `SerializedObject`: поле объявлено `private GridCell[,] _cells`, 2D-массив Unity не сериализует (`SerializedObject` использован для `_cellPrefab`). Всё изолировано в `GridManagerEditorAccess`, при переименовании поля — явный `Debug.LogError`, а не тихий мусор. `Clear()` возвращает `_cells = null` — ровно pre-Awake состояние, поэтому следующий Play Mode инициализирует сетку штатно.
- ⚠️ **`GridManager.Rebuild(w, h, cellSize)` в Edit Mode использовать нельзя**: его `ClearCells()` зовёт `Object.Destroy` (в Edit Mode запрещено) и попутно выставляет `_hasRuntimeOverride`.
- ⚠️ **У заспавненного животного в Edit Mode безопасны только `Place(Vector3)` и `RotateToTarget(Vector3)`.** `AnimalMovement._unitOccupancy` присваивается в `Awake`, поэтому `SetCurrentNode`/`FillNodes`/`ClearNodes`/`SetNewNode` дают NRE. Следствие: `GridManager.PlaceOnGrid` непригоден ЦЕЛИКОМ (внутри `SetCurrentNode`) — инлайнен как `TryFindFreePlacement` → `GetCell` → `Place` → `SetOccupied`. Поворот в рантайме ставит `AnimalMovement.Start()`, которого в Edit Mode нет, — инструмент зовёт `RotateToTarget` сам. `AnimalFacade.Movement` и `.Type` — сериализованные поля, доступны без Awake; `AnimalFacade.Occupancy` присваивается в `Start` → в Edit Mode `null`.
- ⚠️ **Guard обязателен и от Play Mode, и от сохранения сцены** (`BattleSceneBuilderSceneGuard`, `[InitializeOnLoad]`, снимает превью на `PlayModeStateChange.ExitingEditMode` и на `EditorSceneManager.sceneSaving`). Причины разные: (1) клетки лежат на слое `PathNode`, союзники на `Animal` — ровно те два слоя, по которым райкастит `AnimalMovement.TryPlace` (`AnimalMovement.cs:225`), мёртвое превью сломало бы мердж и выбор животных; (2) `BattleSceneBuildRoot` компилируется только под `#if UNITY_EDITOR`, поэтому сохранённая с превью сцена дала бы missing script в плеерном билде. Guard зовёт полноценный `Clear()` (чтобы откатился и `_cells`), а не просто удаляет объект. **Сцена инструментом никогда не сохраняется** — `EditorSceneManager.SaveScene` не вызывается.
- **Тумблера «Build Grid» нет намеренно.** Он был в первой версии и оказался мёртвой опцией: `Build` всегда начинается с `Clear()`, обнуляющего `_cells`, а без сетки размещать юнитов негде — переиспользовать «уже стоящую» сетку невозможно по построению. Опция убрана, а не «починена».
- **Расстановка воспроизводит рантайм КАК ЕСТЬ, включая известные расхождения, а не чинит их**: гейт `HasCellFor` по таблице `GridManager.GetFootprint` (Fox/Hedgehog там 1×2 при фактическом `_unitSize` 1×1 в префабах), ветка `ChickenFlock` 2×2 на 4 курицы, порядок пула. Враги — паритет с `EnemySpawnService.SpawnEnemyAtPosition`: те же клетки, слой `Enemy` только на корне, имя с `_{index}`; `ApplyStats` не зовётся (нужен `AnimalDatabase`, к визуальной композиции отношения не имеет). Превью, которое «лучше» игры, бесполезно как превью.
- **Трекинг созданного — единый root `[AnimalMerge] SceneBuilder` с маркер-компонентом `BattleSceneBuildRoot`**, поиск обходом `GetRootGameObjects()`, а не через поля окна, поэтому превью переживает domain reload. То, что стояло в сцене до запуска, не трогается никогда. Резолв сеток (`BattleSceneReferenceResolver`): `PathfindingInstaller` → `AnimalSpawner` → фолбэк по `Height`.
- **Данные, подтверждённые чтением ассетов с диска** (полезны сами по себе): `LevelStageConfig` не содержит сетки ВООБЩЕ — только список врагов (`_prefab`, `_gridPosition`, `_isBoss`), `_isBossStage`, `_stageNumber` (`Assets/Settings/LevelConfigs/LevelStageConfig_1..4.asset`; `_1` = 6× `EnemyChicken_Temp` — это **x = 1..6 при одном общем y**, а не «клетки 1..6 и 9»; y был 9, с 2026-08-17 равен 11); `Assets/Resources/Levels/Level_1..4.prefab` содержат только `ExtendedLevel` с массивом `_stages`; сетка живёт исключительно в `Main Scene` (**[2026-08-17] одна `GameGrid` (0,0,−2) 8×12**, cellSize 1, Untagged; было `GameGrid` (0,0,0) 8×10 + `MergeGrid` (0,0,−3) 8×2); `PreBattleConfig._startingPool` = `[Cheetah, Fox, Elephant]`; в `Assets/Code` нет ни одного `[ExecuteAlways]`/`[ExecuteInEditMode]`.
- ✅ **[2026-08-17] Поправка: файлы в git с коммита `74fcf89d` вместе с `.meta`** — прежняя запись «не импортированы, `.meta` не сгенерированы» недействительна. Отсмотр Build/Clear и последующего Play Mode в живом редакторе по-прежнему не документирован.
- **Не применённые предложения ревью**: занятость клеток в превью не видна (`GridCell.UpdateVisual` — no-op в Edit Mode, `_meshRenderer` присваивается в `Awake`); `BattleSceneRoot.Find()` зовётся каждый `OnGUI`; `PreBattleConfig` резолвится по хардкод-пути, а не из `BattleInstaller._preBattleConfig`; `BattleSceneReferenceResolver` при переименовании полей молча падает на эвристику по `Height`; семантика пула отличается от `AllySpawnService.RequestSpawn` (рантайм при неудаче стопорится, инструмент пропускает тип и идёт дальше); `EditorUnitSpawner.DirectionVector` — третья копия `DirectionToVector3`.

### Очередность ходов — `Code/Battle/Services/` · `Code/Battle/UI/` (закоммичено, `35a3a029` + `33f3e0a8`)
- 🔴 **Порядок ходов — СНАПШОТ исполнения, а не живой пересчёт.** `TurnExecutor` записывает отсортированный список раунда через `ITurnOrderRecorder`, `ITurnOrderProvider` его отдаёт. Пересчёт по требованию давал бы ложь: юниты ДВИГАЮТСЯ во время своего хода, `Movement.CurrentPathNode.GridPosition` меняется посреди раунда, и уже походивший юнит мог пересортироваться выше ещё не походивших. **Общий принцип для любого будущего UI поверх порядка ходов.**
- 🔴 **Сторона юнита — runtime-принадлежность, а НЕ compile-time тип фасада.** От `EnemyAnimalFacade` наследуются только `PterodactylFacade` / `TRexFacade` / `VelociraptorFacade`, а все три temp-врага (`EnemyChicken_Temp` → `ChickenFacade`, `EnemyCheetah_Temp` → `CheetahFacade`, `EnemyElephant_Temp` → `ElephantFacade`) — от `PlayerAnimalFacade`: один и тот же префаб играет и за союзника, и за врага. `ITurnOrderProvider.GetRoundOrder()` отдаёт `IReadOnlyList<TurnOrderUnit>` (`Unit` + `IsEnemy`), сторона проставляется из того списка, откуда юнит пришёл (и в записанной ветке `_playerTurns`/`_enemyTurns`, и в пре-батл фоллбэке `GetAlivePlayerUnits()`/`GetAliveEnemyUnits()`). **Правило шире этой фичи: любой код, которому нужно «свой/чужой», обязан спрашивать трекер, а не тип.**
- `TurnOrderCalculator` — статический, единственное место с правилом сортировки (союзники `OrderBy(x).ThenByDescending(y)`, враги `OrderByDescending(x).ThenByDescending(y)`), цепочки перенесены из `TurnExecutor` вербатим. Раунд = все живые союзники, затем все живые враги; **инициативы/скорости хода в проекте нет**.
- `TurnOrderProvider` хранит ally-хвост и enemy-хвост раздельно, конкатенирует при чтении, фильтрует `null`/fake-null/`Health.IsDead`, но **не пересортировывает**. `RecordPlayerTurns` заодно снапшотит текущий порядок врагов (иначе хвост колонки пуст во время фазы игрока). При `_hasRecordedRound == false` (пре-батл) — фоллбэк на живой расчёт, он корректен ровно потому, что вне боя никто не движется. `Clear()` — в конце вражеской фазы.
- **Три сигнала**: `TurnRoundStartedSignal { bool IsPlayerSide }` (после записи снапшота, до цикла), `UnitTurnStartedSignal { AnimalFacade Unit }` (после guard на `null`/`IsDead`, до выполнения хода) и `UnitTurnCompletedSignal { AnimalFacade Unit }` (после `await ExecuteSingleUnitTurnAsync`, внутри того же foreach и того же guard'а — Started/Completed всегда парные). Все `.OptionalSubscriber()`. **Исполнение боя байт-в-байт прежнее** — добавлено только публикование фактов наружу.
- ⚠️ **Смерть ВРАГА не проходит через `IUnitTracker`.** `OnUnitRemoved` фаерит `PlayerUnitsChanged` только при `removedFromPlayers`, а на `Health.Died` трекер подписан только для союзников — убитый враг залипал в колонке. Починено НЕ расширением трекера, а `UnitTurnCompletedSignal` + фильтром `Health.IsDead` в провайдере: работает даже пока юнит ещё в трекере и проигрывает анимацию смерти.
- **UI**: `TurnOrderView` (свой пул энтри, `_maxEntries` 6, `CanvasGroup`-fade, репарент под `Canvas` в `Awake` + sibling index 0, чтобы Win/Lose/Victory рисовались поверх) · `TurnOrderEntryView` · `TurnOrderPresenter : IInitializable, IDisposable` с симметричными подписками (`PreBattlePhaseStartedSignal`, `AllySpawnedSignal`, `AllyMergedSignal`, `IUnitTracker.PlayerUnitsChanged`, три новых сигнала, `StateChangedSignal` → Hide на Win/Lose/CampaignVictory). Активный ротируется наверх через `order[(activeIndex + i) % order.Count]`. DTO `TurnOrderEntryData { int Id, Sprite Portrait, bool IsActive, bool IsEnemy }` — вью не знает про `AnimalFacade`.
- ⚠️ **`_hasActiveTurn` отделён от `_activeUnit`**: `UnitTurnCompletedSignal` гасит только подсветку, но НЕ сбрасывает `_activeUnit` — иначе `ResolveActiveIndex` откатится на index 0 и колонка прыгнет к первому юниту раунда между ходами.
- 🔴 **`VerticalLayoutGroup` + `ContentSizeFitter` УДАЛЕНЫ из `TurnOrderView.prefab`** (прежняя запись про «`VerticalLayoutGroup` игнорирует `localScale`, твин на внутреннем `_content`» — НЕДЕЙСТВИТЕЛЬНА). Лэйаут-группа не умеет анимировать перестановку: слоты считает код (`_entryHeight` и `_entrySpacing` сериализованы), энтри позиционируются твином `anchoredPosition`, высоту колонки (`sizeDelta.y`) выставляет сам `Render`. Якоря: корень (0,1)-(0,1) пивот (0,1), энтри анкор (0.5,1) пивот (0.5,1). Масштаб активного — прежний `DOScale`/`OutBack`.
- **Идентичность энтри — `TurnOrderEntryData.Id` = `unit.GetInstanceID()`.** `Render` матчит существующие вью по `Id`, добирает из пула и ретайрит лишние — именно это превращает ротацию в движение вместо перестройки списка.
- 🔴 **Пул никогда не должен отдавать вью, которая ещё числится активной.** Второй fallback-проход `TakeAvailableEntry` исключал только `_next`, но не `_active`, и возвращал вью, всё ещё представляющую другого юнита; `Render` считал её `animated: true`, пропускал `Restore()` и анимировал чужую аватарку — портрет щёлкал, по колонке ехал не тот аватар. Воспроизводилось КАЖДЫЙ ход при юнитах > `_maxEntries` (уровень 1: 9 юнитов при 6 слотах) и при мердже с полной колонкой. **Починено удалением fallback-прохода**: при исчерпанном пуле инстанцируется свежая вью, пул стабилизируется на `_maxEntries + 1`. Признак живости обязан быть identity-aware, а не «не в списке следующих».
- ⚠️ **Общий материал в UI нельзя твинить** — правка `_Saturation` на `UIGrayscale.mat` перекрасила бы сразу все энтри, а per-instance материал аллоцируется на каждый энтри и в этом проекте никем не уничтожается. **Кроссфейд серый↔цветной сделан двумя наложенными `Image`**: нижний `Portrait` с общим `UIGrayscale.mat`, верхний `PortraitColor` без материала, твинится альфа верхнего. Рамка всегда enabled и фейдится по альфе вместо `SetActive`.
- **Реэнтерабельность твинов обязательна**: `Refresh` прилетает пачками (Completed(N) и Started(N+1) фаятся в одном синхронном блоке), поэтому у каждого анимируемого свойства свой твин-филд, киллится перед новым, всё киллится в `OnDisable`/`OnDestroy`. Тайминги сериализованы: `_moveDuration` 0.28/OutCubic, `_colorFadeDuration` 0.25, `_activateDuration` 0.25/OutBack, `_deactivateDuration` 0.2/OutQuad, `_retireDuration` 0.15, `_appearDuration` 0.15 (возврат из пула теперь симметричен выходу), `_activeScale` 1.15.
- ⚠️ **Колонка стоит там, где `AnimalMover` читает тапы** — все `Image` с `raycastTarget = false` плюс `CanvasGroup.blocksRaycasts/interactable = false`, иначе левый верхний угол поля становится мёртвой зоной для выбора животного.
- ⚠️ **`Assets/UI_Sprites/AnimalHeads/*.png` непригодны для grayscale** — все 9 чисто белые силуэты (каждый непрозрачный пиксель ровно `255,255,255`), форма только в альфе; серый от белого = белый. Отсюда `Assets/Code/Editor/AnimalPortraits/AnimalPortraitBaker.cs` (`Tools/Animals/Bake Turn Order Portraits`) — 9 цветных портретов запечены из реальных 3D-префабов в `Assets/UI_Sprites/AnimalPortraits/`; префаб под тип резолвится чтением `AnimalFacade.Type` с корня префаба, не по таблице имён. Портреты **непрозрачные с тёмным фоном** (opaque-шейдеры не пишут альфу надёжно) — `Background`-image из entry-префаба убран.
- **`AnimalConfig.Portrait` — отдельное поле, а НЕ переиспользование `Icon`** (`Icon` потребляет `AnimalStatsPanelPresenter`), `AnimalDatabase.GetPortrait()` = `Portrait ?? Icon`. Попутно починена дыра: у `Pterodactyl` в `AnimalDatabase.asset` был `Icon: {fileID: 0}` — это чинит и стат-панель, и merge-popup.
- **Grayscale** — `Assets/Shaders/UIGrayscale.shader` на базе UI-Default (сохранены stencil, `_ClipRect`, `ZTest [unity_GUIZTestMode]`) + один общий `UIGrayscale.mat` на нижнем `Image` всех энтри: без per-instance материалов и утечек.
- **`Canvas.prefab` НЕ трогали** — колонка это свой префаб, `FromComponentInNewPrefab` + репарент в `Awake` (паттерн `AnimalStatsPanelView.AttachToCanvas`). В `Main Scene.unity` только +2 строки (`_turnOrderPrefab` на `BattleInstaller`).
- Мокап показывает врага наверху колонки, но реальный флоу — `PlayerTurnState` первым, поэтому наверху союзник. Порядок под картинку не подделывали.
- **Проверка**: 0 ошибок компиляции, `TurnExecutorTests` + `BattleEdgeCasesTests` 16/16 (правился только `BattleTestHelper` — у `TurnExecutor` новые ctor-параметры). Play Mode на 11 юнитах в 6-слотовой колонке: **7 вью использовано, 0 несвязанных слотов, 0 нарушений идентичности за 11 ходов, 0 расхождений стороны против `IUnitTracker`**. Новых тестов нет (**TESTS PAUSED**).
- Осознанно не починено: `GetRoundOrder()` аллоцирует на каждый `Refresh` (не горячий путь); `Render` продолжает работать, пока колонка скрыта.

### Вражеский префаб Pteranodon — `Resources/Prefabs/Enemies/` (закоммичено, `c26564b4` + `592671e2`)
- **Обычный (не-вариантный) префаб**, набор компонентов выведен из `T-Rex.prefab`/`Velociraptor.prefab`: `Animator`, `PterodactylFacade`, `AnimalMovement`, `UnitOccupancy`, `AnimalAttack`, `AnimalHealth`, `AnimalAnimator`, `DamagePopupController`; дети `Collider` (слой 23, не-триггерный `SphereCollider`), `AttackPoint`, вложенный инстанс `HealthBar`. Корень: слой 23 (`Enemy`), тег `Untagged`, scale 0.8. Объект со сцены удалён.
- `Code/Animals/Facades/PterodactylFacade.cs` — `: EnemyAnimalFacade`, пустой `InitBehaviours()` по образцу `TRexFacade`. **Нового Zenject-биндинга не требуется** — наследуется `AnimalFacade.Construct(IRandomProvider)`.
- **Анимации**: `Pteranodon_Attack.anim` (копия `Pterodactyl_Jump.anim`, 2.5 с, loop off) с событием `AttackAnimationHandler` на `t = 1.5`; `Pteranodon_Death.anim` синтезирована (кривые заморожены на `t = 0` + поворот `Group/Main` на 90° по Z за 0.9 с, всего 1.2 с) — **нужен арт-пасс**. `Pterodactyl.controller` переписан на месте: 7 параметров как в `Trex.controller`, Idle ⇄ Flap по `MoveSpeed ≷ 0.1`, → Attack по триггеру, **Any State → Death по `IsDead == true`**; удалены wander-булевы пакета и 5 осиротевших сабассетов `AnimatorStateTransition`.
- ⚠️ **`UnitSize {2,1}` НЕ поддерживается стеком сетки.** Спека 06 §2.5 требует 2х1, футпринт был так и сделан, а затем откачен к `{1,1}`. `TargetPositionCalculator.GetAnchorPointsForCell` (`TargetPositionCalculator.cs:255`) ветвится только на `Small (1,1)` / `Medium (1,2)` / `Large (2,2)` (`UnitSize.cs:22-24`) → для `{2,1}` пустой список якорей → `GetPossibleAttackPositions` пуст → `AnimalMovement.Move` = `false` → `TurnExecutor` превращает весь ход врага в no-op (юнит не ходит и не атакует). Плюс `GridManager.GetOccupiedCellsInternal` (`:468-495`) для прямоугольников смотрит только `size.Height` (занимает ОДНУ клетку), тогда как `AdjustPositionToFitBounds` (`:392-403`) `Width = 2` учитывает, а `Utilities.GetMovementOffset` (`Utilities.cs:117-120`) сдвигает меш на полклетки. **Безопасны только 1х1, 1х2 и 2х2.**
- ⚠️ **Анимационное событие `AttackAnimationHandler` обязательно на каждом новом клипе атаки.** `AnimalAttack.Attack(ITarget)` (`AnimalAttack.cs:157`) крутит `while (!_isAttackDone) await UniTask.Yield();`, а флаг ставит только `AttackAnimationHandlerAsync`, куда попадают исключительно по этому событию. Без него ход врага виснет навсегда (известный баг оленя). Ставить на ~55–65 % клипа, exit time исходящего перехода — позже события.
- ⚠️ **`EnemySpawnService.cs:107` ставит только слой КОРНЯ** — дети сохраняют сериализованные слои, поэтому дочерний `Collider` обязан быть авторингом на слое 23, иначе атаки игрока (маска `1 << 23` = `8388608`) в него не попадут. Вражеский `AnimalAttack._mask` = `1048576` (`1 << 20`, Animal).
- **Клипы и контроллер лежат в вендорской папке polyperfect** — тот же прецедент, что у `Trex.controller`/`Velociraptor.controller`. Ре-импорт пакета сотрёт правки (принято осознанно).
- **Найдено, но НЕ починено**: `Velociraptor.controller` объявляет `IsDead` как **Trigger** (`m_Type: 9`), а `AnimalAnimator.DeathAnimation()` зовёт `SetBool`; у `Trex.controller` и `Velociraptor.controller` `m_AnyStateTransitions: []` — разводка Any State → Death есть только у птеранодона.
- `PterodactylStats.asset` на диске — **200 HP / 200 dmg / 3 клетки** (вне скоупа, намеренно не трогали).

### Синхронизация баланса с концепт-доком 3.0 — спека 06 (закоммичено, `088b406f`)
- 🔴 **Балансные числа НЕ доехали до диска и были переприменены 2026-08-02.** Запись «правлены через `SerializedObject`» оказалась ложной: `SerializedObject` пишет в память редактора, и project-wide `SaveAssets` из параллельной сессии выписал поверх своё in-memory состояние. Актуальные значения и способ применения — в записи от 2026-08-02 выше и в спеке 06 §8. **Правило: после балансной правки ассета перечитывать значение с диска, а не доверять успешному `ApplyModifiedProperties()`. Надёжнее всего — править YAML текстом + `AssetDatabase.ImportAsset(path, ForceUpdate)`.**
- ⚠️ **Курица ОСТАЁТСЯ `_unitSize: 1x1`.** 2х2 — это футпринт **операции спавна**, а не размер юнита: каждая курица независима (свои HP, свой ход, своя смерть, свой `ChickenAttack`). Поднять `_unitSize` до 2х2 значило бы превратить стаю в один большой юнит и обесценить мердж-скилл клонирования. Знание о стае живёт в новом `Assets/Code/Animals/ChickenFlock.cs` (`Footprint` 2x2, `Count` 4).
- **`GridManager.TryFindFreePlacement(size, direction, out anchor)` — единственный алгоритм поиска места**, `HasCellFor` и `PlaceOnGrid` оба делегируют сюда. `AdjustPositionToFitBounds` вызывается **до** `CanPlaceUnit`, наружу отдаётся скорректированный anchor. Попутно починен латентный boundary-баг: `PlaceOnGrid` раньше кормил `GetCell` нескорректированной позицией (проверял одну клетку, занимал другую) — не проявлялся только потому, что никто не запрашивал размещение у самой границы.
- **Спавн стаи атомарен**: `IAnimalFactory.SpawnAdditionalChickens(mainChicken)` → `CreateChickenFlock(IReadOnlyList<IGridCell>)`, а `AnimalSpawner.Spawn` уходит на flock-ветку **до** инстанцирования. Нет блока 2х2 → не создаётся ни одной курицы (раньше главная курица оставалась одна).
- ⚠️ **Откат стаи чистит занятость через `created.Movement.ClearNodes()`, а НЕ через `created.Occupancy`.** `AnimalFacade._occupancy` присваивается в `Start()`, который на момент синхронного `InstantiatePrefabForComponent` внутри фабрики ещё не отработал → `Occupancy` был бы `null` ровно тогда, когда нужен откату. `AnimalMovement._unitOccupancy` присваивается в `Awake()`. **Общее правило: внутри фабрики сразу после инстанцирования доступны только `Awake`-инициализированные поля.**
- **Визуал ×0.75 при мердже курицы** — `MergeAnimationConfig._cloneScaleMultiplier` применяется и к клону, и к оригиналу. `MergeScaleAnimator.GrowBy`/`UndoGrowBy` → `ScaleBy`/`UndoScaleBy` + новый `Resync(Vector3)` (закрыт SUGGESTION прошлой фичи про дрейф `_logicalScale`).
- ⚠️ **Два порядковых инварианта.** (1) `_targetAnimal.ScaleAnimator?.Resync(...)` обязан быть **последним** выражением `MergeCommand.RestoreVisualState`, после `MergeView.UndoVisuals(...)`: внутри `UndoVisuals` отрабатывает `ElephantMergeAttribute.Undo()`, который делит `_logicalScale`, и ранний ресинк оставил бы слона на `preMergeScale / 1.5`. (2) Клона надо ужимать **до** `MergeAppearAnimation.Begin` — `Begin` сразу зовёт `CollapseScaleAndAttachGlow`, который перезатирает `localScale`; `ScaleBy(multiplier, 0f, null)` снапит масштаб и одновременно пишет `_logicalScale`.
- **Шанс контратаки вынесен в данные** по образцу `FoxStats`: новый `Code/Data/Animals/HedgehogStats.cs` (100 владелец / 50 унаследованный), ассет сконвертирован in-place (GUID ассета не менялся). `CounterAttack` стал value-agnostic (`int successChance` вместо `bool isOwner`), `HedgehogFacade` читает `AnimalDatabase`, `HedgehogMergeSkill` хранит `_inheritedChance`.
- **`AnimalFactory` почищена от мёртвого DI**: удалены `SetMergeGrid` (из интерфейса и реализации), поля `_mergeGrid`/`_gridManager`, ctor-параметр `[Inject(Id = GridIdentifier.MergeGrid)] IGridManager`. Правок инсталлера не потребовалось — `Container.Bind<IAnimalFactory>().To<AnimalFactory>().AsSingle()` резолвит ctor-аргументы через контейнер. Закрыт старый `//todo Temp, delete later`.
- **Осознанно не сделано**: §2.8 (иглы ежа) — блокер арт; §3 (прогрессия разблокировки животных) — блокер геймдизайн; `AllySpawnService.SpawnAndRegister` шлёт `AllySpawnedSignal` только с `Unit = spawned[0]`, хотя регистрирует все 4 курицы.

### Выбор животного и карточка статов — `Code/Animals/Selection/` · `Code/Animals/UI/` (закоммичено, `534c11a1`)
- **`IAnimalSelectionService` / `AnimalSelectionService` — единственный источник истины о выбранном животном.** Владеет `IMoveRangeHighlighter` (переехал из `AnimalMover`), отдаёт `event Action<AnimalFacade> SelectionChanged` (`null` = сброшено). `IInitializable, ITickable, IDisposable`, биндинг `BindInterfacesAndSelfTo<AnimalSelectionService>().AsSingle()` в `CurrentGameInstaller`.
- ⚠️ **`Tick()` обязателен: `NotifyRemoved()` зовут не все пути уничтожения** — `EnemySpawnService:170`, `AnimalFactory:190`, `ChickenFacade:90/98` уничтожают объект напрямую. Сервис ловит **fake-null** `_selected` в тике, помимо подписки на `AnimalFacade.OnRemoved`.
- ⚠️ **`_selectionAllowed` инициализируется в `true`, а не в `false`** — `GameBootstrapper.Awake()` входит в стейт-машину раньше, чем Zenject-кернел вызовет `Initialize()`, то есть первый `PreBattlePhaseStartedSignal` сервис не услышит. Рестарт работает: `PreBattleState.Enter()` фаерит Started при каждом входе, включая внутрипроцессный.
- **`AnimalMover` — чистый переводчик жеста в намерение**, знания про UI нет. Тап выбирает · повторный тап по тому же снимает · тап по пустому снимает · драг сбрасывает и карточку не показывает · **выбор переживает отпускание пальца**. `Clear()` только на промахе рейкаста и в `BeginDrag` (не на каждом нажатии — иначе мигание карточки и провал подсветки на ~150 мс при переключении между животными). `TryPickAnimal()` обнуляет `_current` первым действием.
- **Карточка** — `AnimalStatsPanelView` + `AnimalStatsPanelPresenter` + `Assets/Prefabs/AnimalStatsPanelView.prefab`, биндинг в `BattleInstaller` (`FromComponentInNewPrefab` + презентер `NonLazy`). Screen-space-overlay, следует за животным (`WorldToScreenPoint` → `ScreenPointToLocalPointInRectangle`, как `DamagePopupController`/`MergePopupController`/`TutorialHandView`). `ResolvePanelPosition` + `CoversAnchor` переключают сторону по **склампленному** результату — на сыром офсете в полосе x ∈ [−393, −123] карточка накрывала описываемое животное.
- **Презентер читает `AnimalFacade.GetDamage()` / `GetCurrentHealth()`, НЕ `AnimalStats`** — мердж мутирует рантайм-значения (`MergeTarget.ExecuteMergeDirectly`). Живое обновление по `Health.HealthChanged`. Зависимости от `SignalBus` у презентера нет — гейт фазы в сервисе.
- **Выбор только для союзников** — враги на слое 23 (`Enemy`), `AnimalMover.CastRay` маскирует слой 20 (`Animal`). Оставлено сознательно: нет утечки информации о радиусе хода врага.
- **Данные и арт**: `AnimalConfig.Icon` + `AnimalDatabase.GetIcon(AnimalType)` (молча `null`), иконки для всех 9 типов в `Assets/UI_Sprites/` (**не в `Resources/`** — тянет ссылка из `AnimalDatabase.asset`). Нарисованы процедурно через `execute_code`. У `Hedgehog` и `Velociraptor` нет кости головы в риге — силуэты в полный рост.
- **Цвета макета**: `AddAnimalButtonView` `#7CB342`/`#5A7F33`, `BattleButtonView` `#F5A623`/`#B0762A`, подписи белые `riffic-bold SDF`; радиус хода — янтарный `{0.96, 0.80, 0.47, 1}` на шести союзных префабах.
- ⚠️ **`EventSystem.IsPointerOverGameObject()` без аргумента — мёртвый код на Android/iOS** (резолвит id мыши `-1`, а `StandaloneInputModule` кладёт тач под `fingerId`). Передавать `Input.GetTouch(0).fingerId` при `touchCount > 0`; безаргументный вызов — только editor/standalone-фолбэк. Проверить остальные точки ввода.
- ⚠️ **Кнопки нельзя перекрашивать через `Image.m_Color`** — источник истины сериализованные поля цвета, `ApplyState()`/`SetInteractable()` затирают `_targetGraphic.color` на каждом `Refresh()`.
- ⚠️ **`mcp__UnityMCP__generate_image` нерабочий** — `configured: false` у fal и openrouter. Фолбэк для плейсхолдер-арта — процедурная растеризация через `execute_code`.

### Пулы спавна и паритет префабов — `Code/Animals/` · `Code/Battle/PreBattle/` (закоммичено, `088b406f`)
- ⚠️ **ПОПРАВКА к прошлой записи: владение пулами откачено обратно в `PreBattleConfig`.** В коммите `088b406f` `PreBattleConfig` получил ОБА массива — `_startingPool` (ур. 1) и `_randomPool` (ур. 2+, `Elephant, Cheetah, Deer, Fox, Hedgehog, Chicken`), — а `AllySpawnService.QueueStartingPool()`/`QueueReinforcements()` читают их оттуда. `AnimalSpawner.cs` объявляет **только** `_gameGrid` (поле `_mergeGrid` удалено 2026-08-17 вместе с сеткой). Решение «единственный владелец — `AnimalSpawner`» из `Decisions/2026-08-01-animal-spawn-pools-and-prefab-parity.md` больше НЕ отражает код.
- ⚠️ **Сериализованные в `Main Scene` `AnimalSpawner._startingPool`/`_randomPool` — МЁРТВЫЕ ДАННЫЕ.** Таких полей в классе нет, Unity вычищает эти строки при каждом сохранении сцены. Прошлая запись требовала их беречь — это было ошибкой: их потеря спавн союзников не ломает, восстанавливать руками бессмысленно. Источник истины — `Assets/Settings/BattleConfigs/PreBattleConfig.asset`.
- Семантика `isStartingPool` сохранена: `PoolExhaustedRule` и `MinAllyCountRule` ведут себя идентично.
- ⚠️ **НИКОГДА не добавлять `TRex`/`Pterodactyl`/`Velociraptor` в пулы** — они объявлены в `AnimalType`, но player-префабов в `Assets/Resources/Prefabs/Animals/` нет, только вражеские в `Prefabs/Enemies/`.
- **Удалён мёртвый код**: `Assets/Code/AutoFight.cs` целиком (весь `Update()` закомментирован, `Move()` без вызывающих, вытеснен `TurnExecutor`) — компонент снят с `TargetFinder` в `Main Scene.unity` и с `Grid Mover` в `Develop.unity`; bookkeeping `IAnimalSpawner.Animals` / `_animals` / `OnAnimalRemoved` / `OnDestroy` — учёт юнитов живёт в `UnitTracker`.
- **`Deer.prefab`** доведён до паритета: `AnimalUpgrade`, `DeerFacade` (9 bone capsules, `_mergeSkillMultiplier` 1.5), `MergePopupController`, дочерний `Merge` (layer 20, trigger `SphereCollider` + `MergeTarget` + `MergeView`), дочерний `AttackPoint`, `MergeVisualSlot(sourceType = Fox)` **как override на вложенном инстансе** `Fox tail Variant` (общий префаб не трогали). Починены `AnimalMovement._animator`, `AnimalAttack._animator`/`_attackPoint`/`_radius`/`_mask` — маска была **0**, Deer не мог найти цель. Без фасада Deer не попадал в `AnimalFactory._animalPrefabs` (`LoadCollection<AnimalFacade>`) и `Create(Deer)` бросал `KeyNotFoundException`.
- ⚠️ **`Deer` вешал ход навсегда**: `TurnExecutor` ждёт `_isAttackDone`, который ставит ТОЛЬКО анимационное событие `AttackAnimationHandler`, а стейт `DAttack Horns 1` указывал на FBX-сабклип без событий. Создан standalone `Deer_Attack_Horns_1.anim` (событие на `t = 0.45`), клип назначен в `AnimalAnimator._attackClip`.
- ⚠️ **`Deer` не мог умереть**: `IsDead` был объявлен **Trigger**, а `AnimalAnimator.DeathAnimation()` зовёт `SetBool`. Конвертирован в Bool, создан `Deer_Death.anim` из сабклипа `DGetHit L` (отдельной анимации смерти в Forest Pack нет — поза-коллапс, Loop Time off), добавлен стейт `DDeath` и переход **Any State → DDeath** (`IsDead == true`, no exit time, `canTransitionToSelf = false`).
- **`Hedgehog.prefab`**: `HedgehogFacade._mergeView` подключён к `MergeView` на дочернем `Collider` (был `{fileID: 0}`, работал через fallback `GetComponentInChildren` с логом ошибки).
- **Геометрия merge-триггеров выведена из `AnimalMovement.TryPlace()`** (`Physics.RaycastAll` сверху по маске `PathNode|Animal`, берётся БЛИЖАЙШИЙ хит с `IRaycastable`): сфера обязана перекрывать свои bone-капсулы и НЕ нависать над соседними клетками. Deer: `radius 1.25 → 1`, `center → {0, 1.45, 0.5}`. Hedgehog: `radius 1.2 → 1.6`, `center → {0, 1.2, 0.4}`. ⚠️ У корня `Hedgehog` `localScale = 0.35` — **сериализованный радиус ≠ мировой**; реальный дефект был недокрытием, а не перекрытием: пробник `centre + 0.4` (`_raycastOffset` у Cheetah/Deer/Elephant) попадал в клетку, и ни одно крупное животное не могло вмерджиться в ежа.
- `Deer._zOffset = -0.5` корректен: меш смещён вперёд (визуальный центр `z 0.747` против `0.195` у Cheetah), офсет приводит его к `0.247`.
- **`AnimalFactory.Create`** — `TryGetValue` + `Debug.LogError` + `return null` вместо необработанного `KeyNotFoundException`; защищены все три вызывающих: `AnimalSpawner.Spawn`, `ChickenMergeSkill`, `MultipleCharacters.cs`.
- **Осознанные пропуски**: у ежа НЕТ donated merge-визуала (иголки запечены в единый `SkinnedMeshRenderer`, отдельного пропа нет), у оленя нет слота `Deer → Deer` (`Antlers 02` — его родные активные рога, слот прятал бы их при Undo). Цена — безобидный warning `[SlotMergeAttribute] No MergeVisualSlot with sourceType=Deer`.

### Флоу победы/поражения — `Assets/Code/Framework/Code/Infrastructure/` (закоммичено, `ce7b3485`)
- **Верхнеуровневый FSM**: `BattleEndState` → Victory ведёт в `WinState` (`Level++`, save, аналитика), оттуда либо прежний Win-баннер с 2-секундным авто-переходом, либо — если кампания пройдена — `CampaignVictoryState` (экран «ANIMALS WON», ждёт RESTART). Defeat ведёт в `LoseState`, который теперь ждёт TRY AGAIN (авто-рестарт удалён). Пер-левельный Win не менялся.
- **Три новых сервиса**: `ICampaignProgressService` (`TotalLevels`, `IsCampaignCompleted`, `ResetToFirstLevel()`), `IGameRestartService` (`RetryCurrentLevel()` / `RestartCampaign()`, гейты по `_stateMachine.ActiveState` отсекают двойные клики и клики из неверного состояния), `IBattleResetService` в `Code/Battle/Services/` (уничтожение союзников + `BattleFlowController.Cleanup()`).
- **Семантика кнопок**: TRY AGAIN — текущий уровень, `Progress.Level` не трогается, шлётся `ReportLevelRestarted()`. RESTART — `Progress.Level = 1`, `Collectables.Clear()`, `Save`, уничтожение выживших союзников, сброс battle-состояния.
- **UI**: `WindowType.CampaignVictory` + слот в `WindowHolder`/`WindowPool`; тупые `DefeatWindowView` / `CampaignVictoryWindowView` (только `event`), клей — `GameResultPresenter : IInitializable, IDisposable`. **Новых сигналов не добавлено** — команды идут внутрь через интерфейсы. Биндинги в `MainSceneInstaller` + одна строка в `BattleInstaller`. Обвязка `Canvas.prefab` и `Main Scene.unity` выполнена.
- ⚠️ **`LevelFactory.LoadCurrentLevel()` больше НЕ заворачивает индекс по `%`.** Пока обёртка стояла, состояние «все уровни пройдены» было непредставимо — кампания молча зацикливалась. Теперь выход за диапазон = `Debug.LogError` + `CurrentLevel = null` + ранний выход из `LoadLevelState.Enter()`. Появился `ILevelFactory.TotalLevelsCount` с ленивым `EnsureLoaded()`. Парная защита — `BootstrapState` при `IsCampaignCompleted` зовёт `ResetToFirstLevel()` + `Save` (релонч на экране победы иначе делал игру непроходимой).
- ⚠️ **`CollectablesData.ResetAmount()` разделён на `RevertLevelAmount()` (`Amount -= LevelAmount`) и `Clear()` (`Amount = 0`).** Старое имя лгало: на пути рестарта кампании метод фактически ничего не сбрасывал.
- ⚠️ **`GameStateMachine.Enter<T>()` шлёт `StateChangedSignal` ДО `state.Enter()`** — иначе вложенный переход (`WinState.Enter()` → `CampaignVictoryState`) приходит подписчикам в обратном порядке.
- **`RestartButton` переведён на `IGameRestartService.RetryCurrentLevel()`** — раньше входил в `LoadLevelState` напрямую и тащил живых юнитов и состояние стадий в новый уровень.
- Переименования сериализованных полей (`WindowHolder`, `MainSceneInstaller`) сделаны с `[FormerlySerializedAs]`.

### Система merge-анимаций — `Assets/Code/Animals/Vfx/` (закоммичено, `51763a96`)
- **Пять маленьких независимых кусков, без фреймворка (KISS)**: `Config/MergeAnimationConfig.cs` (ScriptableObject, секции Elephant Growth / Appear / Glow, `OnValidate` клампит `_glowBandsPerBody ≤ 1/(1+2·_glowBandMargin)`), `MergeScaleTween` (статик-lerp на `UniTask.Yield` + `AnimationCurve` через `LerpUnclamped`), `RendererMaterialOverlay` (материальная логика гепарда, обобщённая параметром `Material skinOverride`), `MergeAppearAnimation` (композиция «glow ‖ scale-in»), `MergeScaleAnimator` (MonoBehaviour-арбитр рутового скейла).
- **Арбитр скейла живёт на ЦЕЛИ, а не в атрибуте.** `MergeScaleAnimator` добавляется в рантайме на корень цели (`GetComponent ?? AddComponent`) и владеет `_logicalScale`. Три эффекта одним ходом: владелец на активной цели → ловушка деактивации источника не действует; два `ElephantMergeAttribute` в одном кадре не дерутся за трансформ (второй домножает логический скейл); undo имеет одно место отмены и порядко-независим. Правок префабов не требует.
- **Доставка конфига — `[Inject] ConstructVisual([InjectOptional] MergeAnimationConfig)` в `VisualMergeAttribute`**, биндинг `FromScriptableObjectResource` в `CurrentGameInstaller` (прецедент `AnimalDatabase`). Метод-инъекция достаёт до НЕАКТИВНЫХ детей префаба, т.к. `AnimalFactory.Create` использует `InstantiatePrefabForComponent`. Все потребители null-гардят конфиг → отсутствие биндинга деградирует к старому мгновенному поведению.
- **Рост слона чисто косметический** — логическое значение переключается синхронно, интерполируется только трансформ. Занятость сетки — из `AnimalMovement._unitSize`, pathfinding — из `UnitSize`/`Direction`, радиус атаки — из статов. Единственный логический потребитель `localScale` — `MergeStateSnapshot`. Undo — мгновенный снап.
- ⚠️ **Правило: оверлей, который может делить рендерер с другим писателем, обязан восстанавливать состояние УСЛОВНО** — по сверке с тем, что он сам записал (`RendererState.AppliedSharedMaterials`, `ReferenceEquals` на glow-инстанс), а не безусловно из снапшота. Реальный сценарий: слот активируется первым, `CollectPaintableRenderers` гепарда подхватывает уже активный рендерер рогов, `Dispose()` слотового оверлея затирал гепардовый скин. В `Dispose()` порядок — restore-then-`DisposeGlow`.
- ⚠️ **Кривые с перелётом — это данные, а не код.** Первая версия ассета уехала с плоским smoothstep, и «перелёт» не воспроизводился, хотя код его поддерживал. Итог — 3-ключевой back-out `(0,0,out 2.2) → (0.65,1.12) → (1,1)`; пик 1.56× слон, 1.114× деталь. **При вынесении тюнинга в SO дефолты ассета — часть фичи, проверять отдельно от кода.**
- ⚠️ **`_appearStartScale` = 0.05, а не 0** — нулевой стартовый скейл схлопывал коллайдеры клона курицы, и свежий клон был невыбираемым для следующего мерджа все 0.35 с.
- **Побочно исправлены три предсуществующих бага**: undo слона в цепочке A→B→C (возвращал `1.5S` вместо `S`), взаимное затирание оверлеев, `Deer.prefab` `SlotMergeAttribute._type: 0 → 2` (`_sourceType` слота «Antlers 02» = 2 (Deer), рога не появлялись никогда, мердж писал warning).
- **Закрыты два SUGGESTION прошлой фичи**: `MergeSweepGlow` теперь `: IDisposable`; `OnValidate` гарантирует `_glowBandsPerBody * (1 + 2 * _glowBandMargin) ≤ 1`.

### Туториалы — модуль `Assets/Code/Tutorial/` (закоммичен, обвязка сцены в коммите)
- **Прикладной слой поверх Battle**: читает состояние (`IUnitTracker`, `IPersistentProgressService`) и сигналы, командует только своим view. Геймплейный код о туториале не знает — единственное исключение — новый `AllyMergedSignal`, который фаерит `MergeTarget.ExecuteMergeDirectly`.
- **Расширяемость**: новый шаг = один класс `ITutorialStep` (`Id` · `CanRun` · `UniTask<bool> RunAsync(CancellationToken)`) + одна строка биндинга в `TutorialInstaller`. `TutorialRunner` править не надо.
- **Старт последовательности — по `PreBattlePhaseStartedSignal` с `IsLevelStart == true`, а НЕ в `Initialize()`.** Zenject зовёт `IInitializable.Initialize` из `MonoKernel.Start`, когда `GameBootstrapper.Awake` только вошёл в `BootstrapState` — `PlayerProgress` там может быть ещё `null`.
- **Выбор пары — по индексу спавна** (`SpawnOrderTargetResolver`, default). Координаты сетки непригодны: позиция зависит от `UnitSize` ранее поставленных юнитов и от `GridConfig`.
- **Прогресс — `ITutorialProgressService` на PlayerPrefs** (ключ `Tutorial.{stepId}`), чтобы не трогать `Framework/`. Сброс прохождения — удалить ключ `Tutorial.MergeHint`.
- ⚠️ **UniTask-адаптер DOTween (`ToUniTask`) в проекте НЕДОСТУПЕН.** `UniTask.DOTween.asmdef` гейтит всё за `UNITASK_DOTWEEN_SUPPORT`, а `versionDefines` эмитит define только для UPM-пакета `com.demigiant.dotween`; здесь DOTween из Asset Store → `DOTweenAsyncExtensions` не компилируется. Ждать твин надо через `UniTask.WaitUntil(...).SuppressCancellationThrow()`.
- ⚠️ **Отписка от `SignalBus` должна быть синхронной относительно teardown контейнера** — отсюда `CancelAwareSignalSubscription<T>`.
- Компромиссы (осознанные): `MergeCommand.Undo()` не фаерит контр-сигнал → отменённый мердж засчитывается; шаг завершается по ЛЮБОМУ `AllyMergedSignal`; биндится ровно один `MergeHintStepConfig` `AsSingle`.

### Merge-свип гепарда (закоммичен, `9f7ed831`)
- **Шейдер `Assets/Shaders/IridescentSweep.shader`** — явный world-режим развёртки: `_SweepWorldAxis` (компонента `w` = флаг) + `_SweepWorldOrigin`. Объектная развёртка для целого животного непригодна — у каждого рендерера свой пивот/поворот/масштаб. При `w = 0` математика бит-в-бит прежняя.
- **Драйвер `MergeSweepGlow.cs`** — обычный C#-класс (НЕ MonoBehaviour), владеет одним рантайм-инстансом материала на всех рендереров цели, каждый кадр пересчитывает объединённый world-AABB, фазу гонит на `UniTask.Yield(PlayerLoopTiming.Update, ct)`.
- ⚠️ **Ловушка: владельца merge-атрибута деактивируют через кадр после `Apply`** (`MergeTarget.cs:113` → `:124`). Корутина бросит исключение, очистку нельзя вешать на `OnDisable` (только `OnDestroy`), `UniTask.Yield`/`Delay` деактивацию переживают.
- ⚠️ **Правило: рантайм-оверлей нельзя снапшотить как «оригинал»** — цепочка мерджей Cheetah A → B → C писала в рендерер `[cheetah, null]` навсегда. Починено фильтром по шейдеру `_glowMaterial.shader` в захвате оригиналов.
- Ограничение Unity (принято): при длине массива материалов больше `subMeshCount` лишний (glow) материал перерисовывает только ПОСЛЕДНИЙ сабмеш.

### Ранее закоммичено (`fdb244a9` loot shine, `6d5356aa` дух смерти, `e9d1cc10` пре-батл фаза)
- **Смерть курицы** — анимация падения уже была нарисована и не подключена; доразведены `Any State → down`/`wing_down`, `IsDead` сменён с Trigger на Bool. Эффект духа (`DeathSpiritSpawner` + `DeathSpiritView`) — запечённая через `BakeMesh` копия меша животного, шейдер `SpiritGhost.shader`. **Визуальный вид на экране НЕ проверен.**
- ⚠️ **Открытое окно Animator молча откатывает правки контроллера, сделанные через API, и откат бывает ЧАСТИЧНЫМ** — на `Deer Animations.controller` выжили назначенный клип и тип параметра, а стейт `DDeath` и все `anyStateTransitions` исчезли, диф усох с 69 строк до ~14. Одного закрытия окна недостаточно: проверять `git diff` после КАЖДОЙ правки контроллера.
- **Loot shine** — `IridescentSweepOverlay.mat` на `SkinnedMeshRenderer` `Fox Tail` внутри `Cheetah.prefab`. `Assets/Animals compilation/Prefabs/Fox tail Variant.prefab` НЕ трогать — общий для 10 префабов.
- **Пре-батл фаза** — обвязка сцены выполнена пользователем и закоммичена.

### ⚠️ Состояние рабочего дерева
- Три прежние незакоммиченные фичи **уехали в коммиты**: баланс/стая курицы и пулы спавна + паритет префабов Deer/Hedgehog — в `088b406f`, карточка статов — в `534c11a1`, поверх лёг `519685df` (health bars).
- ✅ **[2026-08-02] Фича птеранодона закоммичена целиком** (`c26564b4` префаб + фасад + клипы + контроллер, `592671e2` подключение к уровню 2), тулза генерации префабов животных — там же. В рабочем дереве от них ничего не осталось.
- ✅ **[2026-08-02] Чужая правка `PreBattleConfig.asset` из дерева ушла** — на диске снова `_startingPool: [Cheetah, Fox, Elephant]` (0,1,3 → `010000000300000000000000`), файл чистый относительно `088b406f`. Курица В СТАРТОВОМ ПУЛЕ больше НЕ стоит, связанный риск софт-лока не активен.
- ✅ **[2026-08-13] Фича очереди ходов закоммичена целиком** (`35a3a029` — сервисы, сигналы, UI, `AnimalPortraitBaker`, два префаба, шейдер, портреты, сцена; `33f3e0a8` — TagManager). В рабочем дереве от неё ничего не осталось.
- ✅ **[2026-08-17] Battle Scene Builder закоммичен** (`74fcf89d`) вместе с `.meta`. Прежняя запись «untracked, 15 `.cs` без `.meta`» была неверна.
- ⛔️ **[устарело, см. ниже — уехало в `4efd3ae1`] Не закоммичено [2026-08-17]**: удаление `MergeGrid` — новый `Assets/Code/GridPathfinding/DeploymentZone.cs` (+ `.meta`), удалённые `GridIdentifier.cs` (+ `.meta`) и `Settings/GridConfigs/MergeGridConfig.asset` (+ `.meta`), ~30 изменённых `.cs`, `Main Scene.unity`, `GameGridConfig.asset`, `LevelStageConfig_1..4.asset`, плюс `AgentsDocs/Specifications/05_Battle_System_Timings_Specification.md` и `Knowledge/GameDesignerGuide.md`.
- ⛔️ **[устарело — уехало в `5e3d9a55`]** Чужая незакоммиченная правка в дереве, к задаче не относящаяся: `Assets/Code/Animals/UI/MergePopupView.cs`.
- ✅ **[2026-09-07] Удаление `MergeGrid` закоммичено** (`4efd3ae1` «remove merge grid from game»), чужая правка `MergePopupView.cs` — тоже (`5e3d9a55`). Обе прежние записи «не закоммичено» недействительны.
- **Не закоммичено [2026-09-08]**: боевой HUD, итерация 1 **целиком (части 1 + 2)** — untracked `Assets/Code/Battle/UI/EnemyCardPresenter.cs`, `EnemyCardView.cs`, `IEnemyCardView.cs`, `Assets/Code/Framework/Code/UI/Elements/SafeArea.cs`, `Assets/Prefabs/EnemyCardView.prefab`, `Assets/UI_Sprites/Placeholder/` (**7** png, все с рукописными `.meta`); изменены `HealthBarView.cs`, `AnimalStatsPanel{View,Presenter}.cs`, `IAnimalStatsPanelView.cs`, `AllySpawnPool.cs`, `AllySpawnService.cs`, `IAllySpawnPool.cs`, `IAllySpawnService.cs`, `IUnitTracker.cs`, `UnitTracker.cs`, `AddAnimalButtonView.cs`, `BattleButtonView.cs`, `PreBattleHudPresenter.cs`, `LevelView.cs`, `BattleInstaller.cs`, `button_1.png.meta`, `button_2.png.meta`, `Assets/Resources/Canvas.prefab`, `Assets/Scenes/Main Scene.unity`, `Assets/Prefabs/{AnimalStatsPanelView,HealthBar,TurnOrderView}.prefab`. ✅ **Play Mode рабочий, коммитить можно** — прежняя пометка «нельзя без части 2» недействительна. 🔴 **Коммитить только вместе с камерой боя** — `EnemyCardPresenter` подписан на `BattleEndedSignal`, а сигнал заведён в параллельной задаче; `BattleInstaller.cs` изменён обеими задачами, по отдельности не компилируются. ⚠️ Сами `button_1.png` / `button_2.png` — байт-в-байт как в `afaefa1e`, в дифе только их `.meta` (первая попытка стереть декоративные штрихи откачена).
- **Не закоммичено [2026-09-07]**: камера боя — untracked `Assets/Code/Battle/CameraControl/` (3 `.cs` + `.meta`), `Assets/Code/Battle/Config/BattleCameraConfig.cs`, `Assets/Code/Battle/Signals/BattleStartedSignal.cs` и `BattleEndedSignal.cs`, `Assets/Settings/BattleConfigs/BattleCameraConfig.asset` (все с рукописными `.meta`); изменены `PreBattleState.cs`, `BattleEndState.cs`, `BattleStateMachine.cs`, `BattleInstaller.cs`, `Main Scene.unity` (одна строка — ссылка на конфиг). 🔴 **`.meta` написаны руками, редактор их не подтверждал** — перед коммитом нужен asset refresh, иначе Unity может перегенерировать GUID и порвать ссылку в сцене.
- Шум редактора, не относящийся к работе: `ProjectSettings/GvhProjectSettings.xml`, `UserSettings/Layouts/*.dwlt`, `Knowledge/.obsidian/workspace.json`, untracked `.DS_Store`.
- ⚠️ **Ремедиация случайного сохранения `Main Scene`**: `git checkout --` файла, а ЗАТЕМ перезагрузить сцену в редакторе (`EditorSceneManager.OpenScene`) — иначе грязная копия в памяти пересохранит тот же мусор. Ханки TMP `m_fontColor32`/`m_fontSize` в дифе сцены — это churn авто-размера шрифта, а не авторская правка.
- ⚠️ **Общий редактор с параллельной сессией портит ссылки в ассетах**: ссылка `_glowMaterial` в `MergeAnimationConfig.asset` один раз обнулилась — project-wide `SaveAssets` выписал на диск in-memory состояние, где она уже была null. Переназначена и проверена.

Открытые вопросы:
- 📋 **[2026-09-08] «Kickback» в коде отсутствует** — по числам похоже на `CounterAttack` (у ежа 50%), нужно подтверждение владельца. Строки перков в карточках пока **статические в префабе**, оба презентера шлют `null` (контракт: `null` = не трогать лейблы, пустой список = погасить). Реальные проценты требуют расширения `IAbility` — вторая итерация HUD.
- 📋 **[2026-09-08] Иконка на HP-баре показывается ВСЕМ врагам, а не только боссам.** Переход на boss-only — правка одного условия, `AnimalFacade.IsBoss` и `StageEnemyConfig.IsBoss` уже существуют.
- 📋 **[2026-09-08] Из UI пропал тумблер вибрации** — `Taptic Button` скрыт (`SetActive false`) как отсутствующий на макете, вместе с `Coins Count`, `Heal All Units` и дублем `Restart`. Ничего не удалено, вернуть = снять галку.
- 📋 **[2026-09-08] Колонка очередности ходов (`TurnOrderView`) на макете отсутствует**, но оставлена как живая функциональность — только сдвинута на `m_AnchoredPosition (36, -320)` из-под safe area.
- 📋 **[2026-09-08] Весь арт в `Assets/UI_Sprites/Placeholder/` временный** (7 png: `icon_lightning`, `icon_boss_skull`, `icon_undo`, `avatar_ring`, `hpbar_hatch`, `icon_attack_paw_white`, `icon_health_heart_white`) — художник заменит.
- 🔴 **[2026-09-08] Предсуществующие падающие тесты, к боевому HUD не относятся**: 4 в `HealthBarViewTests` (подтверждено откатом `HealthBarView.cs` на HEAD — те же 4 падения), плюс падения в `AnimalAttackPostAbilityTests`, `RetreatAbilityTests`, `TargetPositionCalculatorTests`. Ничего не отключалось и не чинилось — отдельная задача.
- 📋 **[2026-08-17] Зона деплоя визуально ничем не отличается от боевого поля** — раньше отличие давала отдельная `MergeGrid`. Нужно геймдизайнерское решение по выделению строк 0–1.
- 📋 **[2026-08-17] `DeploymentZone.Depth` — константа в коде**, тогда как размеры сетки редактируются дизайнером в `GameGridConfig`. Кандидат на перенос в `GridConfig`.
- 📋 **[2026-08-17] `UnitRepositioningService` кастует `IGridManager` к конкретному `GridManager`** — `PlaceOnGrid` и `HasCellFor` просятся в интерфейс следом за поднятым туда `TryFindFreePlacement`.
- 📋 **[2026-08-17] `.DS_Store` не в `.gitignore`.**
- 🔴 **[2026-08-17] Два предсуществующих красных кластера тестов, подтверждённых сверкой с `git show HEAD` — отдельная задача, НЕ регрессия единой сетки**: (1) `AnimalMovementRetreatTests` (3) — сигнатура `[Test] public async Task`, NUnit её отвергает, тесты не проходили никогда; (2) `TargetPositionCalculatorTests` (25) + `EndToEndMovementTests` (4) — `GridTestHelper.CreateMockGrid` и `PlayModeTestHelper.CreateMockGrid` стабят только `bool`-перегрузку `CanPlaceUnit`, а `TargetPositionCalculator` зовёт перегрузку с `HashSet<Vector2Int>` → NSubstitute возвращает `false` → все якоря отвергаются.
- 📋 **[2026-08-13, поправка 2026-08-17] Battle Scene Builder закоммичен** (`74fcf89d`, `.meta` на месте), но отсмотр Build/Clear и последующего входа в Play Mode в живом редакторе так и не документирован. После перевода на единую сетку это стоит сделать заново.
- 📋 **[2026-08-02] По колонке очередности оставлено осознанно:** `ITurnOrderProvider.GetRoundOrder()` аллоцирует список на каждый `Refresh` (не горячий путь — несколько раз за ход, не каждый кадр); `TurnOrderView.Render` продолжает считать слоты и гонять твины, пока колонка скрыта (`alpha = 0` после Win/Lose). Оба — кандидаты на чистку, если колонка обрастёт логикой.
- 📋 **[2026-08-02] Сторона по типу фасада — проверить остальной код.** `unit is EnemyAnimalFacade` лжёт на трёх temp-врагах (`EnemyChicken/Cheetah/Elephant_Temp` наследуются от `PlayerAnimalFacade`) — в колонке починено через `TurnOrderUnit.IsEnemy`, но любой другой потребитель такой проверки содержит тот же баг. Фундаментальное решение — сторона как поле рантайм-состояния юнита либо форк префабов, решение за владельцем.
- 📋 **[2026-08-01] Из спеки 06 остались пункты с внешними блокерами:** §2.8 «иглы ежа» — нужен арт (иголки запечены в единый `SkinnedMeshRenderer`, отдельного пропа нет); §3 «прогрессия разблокировки животных» — нужно решение геймдизайна; §2.5 футпринт птеродактиля 2х1 — блокер код. Источник истины — см. раздел «🎯 Источник истины по геймдизайну и балансу» выше.
- 📋 **[2026-08-02] Не проверено в Play Mode после пересверки баланса:** ёж со скоростью 1 и контратакой 50% (он не атакует сам — при 50% половину столкновений не делает ничего, роль танка-контратакера может просесть), курица с 25 HP (стая = 100 HP), птеродактиль с ходом 5. Все три — заметные изменения силы юнитов.
- ⚠️ **10-е расхождение, не описанное в спеке 06:** `GridManager.HasCellFor` мапит `Fox => 1x2` и `Hedgehog => 1x2`, тогда как оба префаба сериализованы как `_unitSize: 1x1` и концепт-док тоже говорит 1х1. Проверка резервирования требует вдвое больше места, чем юниты реально занимают → `AllySpawnService.CanSpawn` возвращает `false` чаще необходимого. Оставлено как есть — нужно явное решение владельца.
- ⚠️ **[2026-08-01, поправка] Префаб птеранодона теперь ЕСТЬ** (`Resources/Prefabs/Enemies/Pteranodon.prefab`), но требование спеки 06 §2.5 «`_unitSize` 2х1» **заблокировано**: `{2,1}` не поддерживается ни `TargetPositionCalculator`, ни `GridManager` — юнит становится молча неиграбельным. Разблокируется только расширением `UnitSize`/`GridManager`/`TargetPositionCalculator`. Птеродактиль / T-Rex / велоцираптор по-прежнему не должны попадать ни в один спавн-пул (player-префабов у них нет).
- ✅ **[2026-08-02] Pteranodon подключён к `LevelStageConfig_2.asset`** (`592671e2`) — теперь спавнится на уровне 2. Death-клип по-прежнему синтезирован программно и ждёт арт-пасса; в Play Mode бой с ним не отсматривался.
- ✅ **[2026-08-02, закрыто] `PterodactylStats.asset` приведён к доку — 350 HP / 200 dmg / 5 клеток**, проверено на диске. Поведение полёта («летит на 5, атакует, возвращается на место; если цели нет — всё равно слетать вхолостую») **не реализовано**: у птеранодона обычный `AnimalMovement` и пустой `InitBehaviours()`, то есть рядовой наземный юнит с дальностью 5.
- ⚠️ **Писать int-поля через `SerializedProperty.floatValue` — тихий no-op, на диск уходит 0.** Ловилось на `AnimalMovement._sizeEffectY` и `_unitSize.Width/Height`. Использовать `intValue`, либо править YAML префаба текстом и звать `AssetDatabase.ImportAsset` (надёжнее всего); после сохранения перечитывать YAML с диска.
- 🔴 **[2026-08-01, риск стал живым] `PoolExhaustedRule` + курица в стартовом пуле = потенциальный софт-лок.** Раньше софт-лока не было (`[Cheetah, Fox, Elephant]`), но в рабочем дереве `PreBattleConfig.asset._startingPool` изменён на **`[Deer, Chicken, Hedgehog]`** — курица теперь В СТАРТОВОМ ПУЛЕ. `HasFreeCellFor(Chicken)` требует полный блок 2х2, поэтому застрявшая курица навсегда заблокирует кнопку Battle на уровне 1. Нужен escape-hatch или правило гарантированного места — **проверить в Play Mode до коммита этого ассета**. ✅ **[2026-08-02, поправка] правка из дерева ушла, на диске снова `[Cheetah, Fox, Elephant]` — риск на сегодня не активен, но вернётся при любой попытке положить курицу в стартовый пул.**
- 📋 **[2026-08-02] Стартовый ростер не соответствует доку.** Док: «Сначала открыто 3 животных — слон, гепард, ёж». В дереве `[Deer, Chicken, Hedgehog]`, до чужой правки было `[Cheetah, Fox, Elephant]` — доку не равно ни то, ни другое. Приведение к **`[Elephant, Cheetah, Hedgehog]`** снимает заодно и риск софт-лока выше (курица уходит из стартового пула). **[2026-08-02, поправка] чужая правка откачена, на диске `[Cheetah, Fox, Elephant]` — доку по-прежнему не равно (док хочет `[Elephant, Cheetah, Hedgehog]`).** Не трогал: решение за владельцем. Отдельно — расхождение внутри дока: спавн-логика называет базовой тройкой «слон/гепард/**бык**», а быка нет ни в таблице животных, ни в проекте.
- 📋 **[2026-08-02] Механики из дока, не сверенные с кодом** (нашлись при пересверке, требуют проверки): «на поле мерджа курица одна, при выкладке — четыре» (сейчас `AnimalSpawner.Spawn` создаёт четырёх сразу — надо понять, на каком поле); потолок «не более 3-4 скиллов/апгрейдов» на юните; правило мерджа одинаковых животных «жизни и дамаг складываются и умножаются на 0.75».
- **Спавн курицы на тесной сетке и визуал ×0.75 при мердже НЕ проверены в Play Mode** — нужен ручной прогон.
- **Прогрессия не соответствует доку**: док говорит «новые животные открываются за победу над боссами, в первой итерации захардкожены по уровням», у нас плоский `Random.Range` по всем шести с уровня 2. Решение не принято, нужен геймдизайн. Спека 06, §3 — намеренно не реализовано.
- ⚠️ **[2026-08-01, поправка] Стартовый ростер живёт в АССЕТЕ, а не в сцене.** Оба пула вернулись в `PreBattleConfig` (`088b406f`), источник истины — `Assets/Settings/BattleConfigs/PreBattleConfig.asset`. Прежняя запись про «ростер в сцене» и требование беречь `AnimalSpawner._startingPool`/`_randomPool` при сохранении `Main Scene` **недействительны**: таких полей в классе нет, Unity стирает эти строки при каждом сохранении сцены.
- ⚠️ **Рестарт посреди боя**: юниты теперь реально уничтожаются, а `PlayerTurnState`/`TurnExecutor` не отменяют ход по `Exit`. `TurnExecutor` пропускает уничтоженных юнитов между итерациями, но уже начатый `await unit.AttackInstance.Attack(...)` может повиснуть или залогировать missing-reference. Путь был racy и до правки; полное решение — async `ResetForNewRun()` + `CancellationToken` в battle turn states, вынесено за скоуп.
- `LoseState` меняет `Progress.Collectables` без `Save` — штраф в монетах теряется при релонче. Предсуществующее, но новый retry-флоу делает это достижимым намного чаще.
- `UnitTracker.Reset()` логирует два `Debug.LogWarning`, один с полным `StackTraceUtility.ExtractStackTrace()` — теперь это штатный пользовательский путь (каждый Try Again / Restart) на мобильном таргете.
- `RestartCampaign()` не шлёт аналитику, тогда как `RetryCurrentLevel()` зовёт `ReportLevelRestarted()`.
- `LoadProgressState` остаётся недостижимым состоянием (прогресс грузится в конструкторе `PersistentProgressService`) — флоу намеренно не перепроводили.
- Кнопки Try Again / Restart — обычные `UISprite`-прямоугольники с TMP-подписями, арт не подставлен.
- Расширение маски рейкаста `AnimalMover.CastRay` на слой `Enemy` создаст утечку информации о радиусе хода врага — только вместе с осознанным геймдизайнерским решением.
- Иконки голов ежа и велоцираптора — силуэты в полный рост (в риге нет кости `Head`); ждут либо правки рига, либо ручного арта.
- Тесты: действует политика **TESTS PAUSED**. По туториалу, шейдерам, эффекту смерти, merge-свипу и merge-анимациям тесты не писались. Нет теста на `PreBattleHudPresenter`.
- Туториал ещё не проверен в рантайме.
- Не применённые SUGGESTION по merge-анимациям (дрейф `_logicalScale` закрыт — `MergeScaleAnimator.Resync(Vector3)` добавлен 2026-08-01): `GrowScaleMultiplier` — балансная величина слона в общем конфиге, по конвенции проекта место в `ElephantStats : AnimalStats`, плюс `FallbackScaleMultiplier = 1.5f` дублирует дефолт ассета; `SlotMergeAttribute.Apply` рано выходит ДО `StopAppearAnimation()`, если на новой цели нет слота; `MergeScaleAnimator._cancellation` не диспозится на happy path; `MergeAppearAnimation.Begin` разыменовывает `_config` без null-гарда; `OnValidate` клампит `_glowBandsPerBody` разрушительно; три новых `GetComponent`/`AddComponent` вне `Awake`.
- Не применённые SUGGESTION по merge-свипу: `_bandMargin = 0.5` уводит полосу полностью за силуэт до свопа; `_cts` не диспозится на happy path.
- Не применённый SUGGESTION по шейдеру: overdraw — лишний аддитивный проход полного меша в transparent-очереди ломает early-Z на тайловых мобильных GPU.
- `AnimalHealth.Die()` по-прежнему на корутине (`StartCoroutine(DestroyWithDelay())`) — нарушение правила «только UniTask», предсуществующее.
- 4 предсуществующих варнинга ассет-пака в `ChickentwoLayerController` и 5 ошибок параметров того же контроллера из `EnemySpawnService.cs:86`.
- Не применённые SUGGESTION пре-батл фазы: `BattleButtonView._hideUntilReady` по умолчанию `false`; `PreBattleHudPresenter` берёт конкретный `StartBattleService`, а не интерфейс; `AllySpawnService.RequestSpawn` делает dequeue до получения результата спавна.
- Предсуществующие красные тесты (НЕ регрессии): `TargetPositionCalculatorTests` (25) + `EndToEndMovementTests` (4) — **причина найдена 2026-08-17**: хелперы `GridTestHelper.CreateMockGrid` / `PlayModeTestHelper.CreateMockGrid` стабят только `bool`-перегрузку `CanPlaceUnit`, а `TargetPositionCalculator` зовёт перегрузку с `HashSet<Vector2Int>` → NSubstitute возвращает `false`, все якоря отвергаются; `AnimalMovementRetreatTests` (3) — **причина найдена**: `[Test] public async Task`, NUnit такую сигнатуру отвергает; плюс 4 `HealthBarViewTests`, 3 `DamagePopup*`. В Console остаётся предсуществующий NRE `UnityEditor.Graphs.Edge.WakeUp` от аниматор-контроллеров.

## Ключевые решения
Подробности в `Knowledge/Decisions/`. Краткий список:
- [2026-06-27] Merge popup показывается ТОЛЬКО при cross-type мердже (когда реально выдан новый скилл) и ключуется по типу SOURCE (потреблённого) животного; событие `Merged` гейтится флагом `grantedNewSkill` в `ExecuteMergeDirectly`, минует undo → `Decisions/2026-06-27-merge-popup.md`
- [2026-06-27] Подсветка хода вынесена в отдельную сущность (SRP) — `AnimalMovement` не нагружали; drag vs hold различается порогом смещения в пикселях, IInputService не трогали → `Decisions/2026-06-27-move-range-highlighter.md`
- [2026-06-27] Per-animal тюнинг-данные способностей (вероятности Dodge для Fox) хранятся в подклассе `AnimalStats` (`FoxStats`), а НЕ в общем `AnimalConfig` — данные живут со статами животного и не «протекают» в конфиги остальных; `Dodge` стал value-agnostic (`int successChance`), owner/inherited решается на стороне вызова → `Decisions/2026-06-27-fox-dodge-stats.md`
- [2026-07-30] `GridConfig` доставляется в `GridManager` ссылкой в Inspector, а НЕ через Zenject-инжект — размеры нужны в `OnDrawGizmos` в edit-mode, до `Awake` и до создания DI-контейнера; `IGridManager` намеренно не расширялся (blast radius 3 файла вместо 29) → Decisions/2026-07-30-grid-config.md
- [2026-07-31] Пре-батл фаза разложена по слоям: команды идут внутрь через интерфейсы (view → `PreBattleHudPresenter` → `IAllySpawnService`), факты наружу через `SignalBus`; новое условие старта боя = один класс `IBattleReadinessRule` + одна строка биндинга; стартовый пул вынесен в `PreBattleConfig` → Decisions/2026-07-31-pre-battle-phase-architecture.md
- [2026-07-31] Гейт старта боя считается по high-water mark союзников за фазу, а не по живому счётчику — иначе мердж двух союзников в одного (ядро механики) вешал софт-лок с обеими мёртвыми кнопками; `IUnitTracker` стал реактивным (`PlayerUnitsChanged`) → Decisions/2026-07-31-min-ally-count-high-water-mark.md
- [2026-07-31] Эффект свечения сделан аддитивным оверлеем во **втором слоте материала** (базовый lit-материал не трогается) и целиком на `_Time.y` — без C#-контроллера, Zenject-биндинга и сигналов; `_SweepTiling` меряется в «полосах на объект» через `_SweepAxisExtent`, иначе полоса выходит шире меша и волна вырождается в мигание → Decisions/2026-07-31-iridescent-sweep-shader.md
- [2026-07-31] Смерть курицы: перед тем как «делать анимацию», выяснилось, что она уже нарисована и не подключена — правка свелась к доразводке существующих стейтов (`Any State → down`/`wing_down`, `IsDead` → Bool + `canTransitionToSelf = false`); дух сделан запечённой через `BakeMesh` копией меша самого животного, а не отдельным артом, поэтому переносится на любое животное; `BakeMesh` и `Renderer.material` создают объекты, которые НЕ собираются GC — без `Destroy` в `OnDestroy` каждая смерть течёт → Decisions/2026-07-31-chicken-death-spirit.md
- [2026-08-01] Merge-свип: синхронные `Apply`/`Undo` сохранены, асинхронная последовательность запускается через `.Forget()` (иначе `ApplyAsync` расползся бы на 7 файлов и сломал `MergeCommand.Execute() : bool`), а таймлайн крутится на `UniTask.Yield(PlayerLoopTiming.Update)`, потому что владельца атрибута деактивируют через кадр после `Apply`; развёртка полосы вынесена в явный world-режим шейдера (`_SweepWorldAxis.w`), рантайм-оверлей отфильтровывается при захвате «оригинальных» материалов → Decisions/2026-08-01-merge-sweep-glow.md
- [2026-08-01] Туториал вынесен в модуль-надстройку `Code/Tutorial/` над Battle: шаг = один класс `ITutorialStep` + строка биндинга, `RunAsync` сам ждёт свой триггер и возвращает `bool`; последовательность стартует по `PreBattlePhaseStartedSignal` (в `Initialize()` `PlayerProgress` ещё `null`); пара животных выбирается по индексу спавна `IUnitTracker`, а не по координатам сетки; завершение шага — по новому `AllyMergedSignal`, а не по событиям `MergeTarget`; прогресс — отдельный PlayerPrefs-сервис, чтобы не трогать `Framework/` → Decisions/2026-08-01-tutorial-hand-merge-hint.md
- [2026-08-01] Система merge-анимаций: арбитр рутового скейла `MergeScaleAnimator` живёт на ЦЕЛИ, а не в атрибуте — цель остаётся активной (ловушка деактивации источника не действует), два `ElephantMergeAttribute` в одном кадре домножают общий `_logicalScale` вместо драки за трансформ, undo становится порядко-независимым и структурно чинит предсуществующий баг цепочки A→B→C; тюнинг вынесен в `MergeAnimationConfig` (`FromScriptableObjectResource` + `[InjectOptional]` метод-инъекция, достающая до неактивных детей префаба), все потребители null-гардят его; оверлей, делящий рендерер с другим писателем, восстанавливает состояние условно — по сверке с тем, что записал сам → Decisions/2026-08-01-merge-animation-system.md
- [2026-08-01] Флоу победы/поражения: «кампания пройдена» — вычисляемое `Progress.Level > TotalLevels`, а не сохранённый флаг; предусловием фичи стало снятие обёртки `%` в `LevelFactory.LoadCurrentLevel()` (пока она стояла, состояние было непредставимо — кампания молча зацикливалась), парная защита от релонча — сброс в `BootstrapState`; клики Try Again / Restart идут внутрь как команды через `IGameRestartService` (View → `GameResultPresenter` → сервис, новых сигналов ноль), двойные клики отсекаются гейтом по `_stateMachine.ActiveState`, а не булевым флагом; `CollectablesData.ResetAmount()` разделён на `RevertLevelAmount()`/`Clear()`, потому что имя лгало и на пути рестарта кампании метод ничего не сбрасывал → Decisions/2026-08-01-victory-defeat-flow.md
- [2026-08-01] Пулы спавна: единственным владельцем стал `AnimalSpawner` (`_startingPool` уровня 1 + `_randomPool` уровня 2+), `PreBattleConfig` лишился `StartingPool` и оставил только readiness/экономику, `AllySpawnPool` перестал владеть конфигурацией — стартовый ростер сознательно переехал из SO-ассета в scene-компонент ради одного источника правды; ранее записанное «намеренное дублирование» оказалось нераспознанным разделением по фазе и ликвидировано. Смерть Deer разведена через **Any State → DDeath** (вариант «только из Idle», как у Cougar/Fox, не проигрывается при смерти в движении или атаке), геометрия merge-сфер выведена из `AnimalMovement.TryPlace()`, а не подобрана на глаз → Decisions/2026-08-01-animal-spawn-pools-and-prefab-parity.md
- [2026-08-01] Выбор животного вынесен в отдельный `IAnimalSelectionService` (развитие решения про `MoveRangeHighlighter`): он забрал `IMoveRangeHighlighter` у `AnimalMover` и стал единственным местом, где перечислены ВСЕ причины сброса — иначе подсветка и карточка статов разъезжались бы на каждом новом пути очистки; гейт пре-батл фазы живёт в сервисе, а не в презентере (иначе view командует сервисом, и каждый будущий потребитель дублирует подписку на сигналы), `_selectionAllowed` дефолтится в `true`, т.к. `GameBootstrapper.Awake()` опережает `Initialize()` кернела; презентер читает рантайм-значения `AnimalFacade`, а не `AnimalStats`, потому что мердж их мутирует; иконка легла на `AnimalConfig`, а не на подкласс `AnimalStats` → Decisions/2026-08-01-animal-selection-and-stats-panel.md
- [2026-08-01] Стая курицы: префаб остаётся `_unitSize: 1x1`, а 2х2 — футпринт **операции спавна**, а не размер юнита (иначе четыре независимые курицы схлопнулись бы в один большой юнит и мердж-скилл клонирования обесценился), знание о стае живёт в `ChickenFlock`; спавн атомарен — ветвление до инстанцирования вместо отката, а `GridManager.TryFindFreePlacement` стал единственным алгоритмом поиска места (`HasCellFor` и `PlaceOnGrid` делегируют, попутно починен латентный boundary-баг); откат чистит занятость через `Movement.ClearNodes()`, т.к. внутри фабрики сразу после инстанцирования доступны только `Awake`-инициализированные поля; в конфликте «заметка Decisions vs концепт-док» по числам побеждает док — заметка 2026-06-27 фиксировала рефакторинг, а не баланс → Decisions/2026-08-01-chicken-flock-and-balance-sync.md
- [2026-08-01] Вражеский Pteranodon собран как ОБЫЧНЫЙ префаб (паритет с `T-Rex`/`Velociraptor` важнее наследования от FBX), а футпринт из спеки 06 §2.5 сделан 2х1 и откачен к 1х1: `UnitSize {2,1}` не совпадает ни с одной веткой `TargetPositionCalculator.GetAnchorPointsForCell`, поэтому враг молча теряет и ход, и атаку — безопасны только 1х1, 1х2 и 2х2; клипы и переписанный контроллер положены в вендорскую папку polyperfect по существующему прецеденту (ре-импорт пакета их сотрёт — цена принята); на новом клипе атаки обязательно событие `AttackAnimationHandler`, иначе `AnimalAttack.Attack` крутит `while (!_isAttackDone)` вечно → Decisions/2026-08-01-pteranodon-enemy-prefab.md
- [2026-08-02] Очередь ходов: UI показывает **снапшот раунда, который `TurnExecutor` реально итерирует** (`ITurnOrderRecorder` пишет, `ITurnOrderProvider` читает и фильтрует мёртвых, но не пересортировывает), а не живой пересчёт — юниты движутся во время своего хода, ключи сортировки меняются посреди раунда, и уже походивший юнит пересортировывался бы выше ещё не походивших; read/write разделены на два интерфейса при одной реализации, бой публикует наружу только факты (`UnitTurnStartedSignal` / `TurnRoundStartedSignal`, оба `.OptionalSubscriber()`); портреты запечены из реальных 3D-префабов новой тулой `AnimalPortraitBaker`, потому что существующие `AnimalHeads` — чисто белые силуэты и grayscale их не берёт; `AnimalConfig.Portrait` заведён отдельным полем от `Icon` (`Portrait ?? Icon`), колонка — свой префаб с репарентом под `Canvas` в `Awake`, общий `Canvas.prefab` не тронут → Decisions/2026-08-02-turn-order-column.md
- [2026-08-02, дополнено] Очередь ходов, доработка: **сторона юнита — runtime-принадлежность, а не compile-time тип фасада** (один префаб с `ChickenFacade` служит и союзником, и врагом, поэтому `unit is EnemyAnimalFacade` врало на 6 из 11 юнитов первого уровня — спрашивать надо трекер, через `TurnOrderUnit { Unit, IsEnemy }`); **пул UI-вью никогда не отдаёт вью, числящуюся активной** — иначе анимация едет на чужой сущности, признак живости обязан быть identity-aware; смерть врага доведена до UI новым `UnitTurnCompletedSignal`, а НЕ расширением `IUnitTracker`; под плавную ротацию `VerticalLayoutGroup` УДАЛЁН (лэйаут несовместим с анимированной перестановкой), а общий материал не твинится — кроссфейд делается двумя наложенными `Image` → Decisions/2026-08-02-turn-order-column.md
- [2026-08-13] Battle Scene Builder: превью уровня в Edit Mode строится **засевом приватного `GridManager._cells` рефлексией**, а не `[ExecuteAlways]` на рантайм-компонентах и не второй копией алгоритма раскладки — иначе `CanPlaceUnit` итерирует пустой список клеток, возвращает `true` для любой позиции, и все союзники молча схлопываются в `(0,0)` без единой ошибки в консоли; `GridManager.PlaceOnGrid` инлайнен, потому что внутри `SetCurrentNode`, а `_unitOccupancy` присваивается в `Awake` (в Edit Mode безопасны только `Place` и `RotateToTarget`); инструмент воспроизводит рантайм как есть, включая известные расхождения (`GetFootprint` 1×2 у Fox/Hedgehog, стая курицы 2×2), потому что превью, «лучшее» чем игра, бесполезно как превью; превью снимается guard’ом и на `ExitingEditMode`, и на `sceneSaving` — клетки и юниты лежат на слоях `PathNode`/`Animal`, по которым райкастит `AnimalMovement.TryPlace`, а маркер-компонент компилируется только под `#if UNITY_EDITOR` и дал бы missing script в плеерном билде → Decisions/2026-08-13-battle-scene-builder.md
- [2026-08-17] Единая сетка: двусмысленность координат («клетка (3,1)» существовала на обеих сетках, `GridCell.Equals` сравнивает по X/Y) вылечена **удалением второй системы координат**, а не аккуратностью в каждой точке сравнения — три бага (телепорт союзника на первом ходу, костыль `MoveRangeHighlighter.IsOnGameGrid`, поиск клетки под клона курицы на мерж-сетке) закрылись структурно; геометрия выбрана по Варианту A (`GameGrid` 8×12 в z = −2, враги y = 9 → 11) ради визуальной стабильности мира, ценой +2 клеток логической дистанции; зона деплоя вынесена в явный `DeploymentZone` (`Depth = 2`), а её границу теперь держит **гейт `FitsInDeploymentZone` в `TryFindFreePlacement`**, потому что `CanPlaceUnit` выход за границы не ловит в принципе (`null`-клетки молча отбрасываются) — раньше границу держала сама высота `MergeGrid`; **при снятии неявного ограничения искать надо не только код, который его проверял, но и код, который на нём молча ехал** (`ChickenMergeSkill` найден только на ревью) → Decisions/2026-08-17-single-game-grid-deployment-zone.md
- [2026-09-07] Приближение камеры при старте боя: зум сделан **долли** (сдвиг Transform вдоль `forward`), а не анимацией FOV — дочерняя `HealthBarCamera` со своим FOV рассинхронизировала бы все хелсбары с юнитами; триггер — `BattleStartedSignal` из `PreBattleState.StartBattleAsync()`, а не клик по Fight (readiness-правила могут отклонить старт); отъезд слушает и `BattleEndedSignal`, и `PreBattlePhaseStartedSignal`, потому что сцена не перезагружается; твин — ручной UniTask-цикл, так как await'абельный DOTween собран только под Android → Decisions/2026-09-07-battle-camera-zoom.md
- [2026-09-07] Боевой HUD, итерация 1, часть 1 (C#-слой; **завершено 2026-09-08, см. следующую строку**): **разведка устройства UI оказалась дороже самой вёрстки** — `AnimalStatsPanelView` не world-space, а **screen-space** с проекцией `WorldToScreenPoint` в `LateUpdate` (единственный настоящий world-space UI — `HealthBar.prefab` со своей `HealthBarCamera`); **врага нельзя выбрать тапом** (`AnimalMover.CastRay()` кастует маску `Animal`, враги на слое `Enemy` 23), поэтому карточка врага получила свой презентер от `IUnitTracker` + сигналов боя вместо `IAnimalSelectionService`, и показывается один раз за бой с перенацеливанием, а не прячется на каждом ходу (иначе мигает пересозданным DOTween-сиквенсом); **TMP молча игнорирует `Graphic.material`** — grayscale на FIGHT кладётся только на не-TMP `Graphic`, авторские материалы восстанавливаются из кэша, а не затираются в `null`, и тинт не домножается поверх grayscale; серый спрайт FIGHT не рисуется принципиально (`ColorTint` даёт коричневый, материал — металлик); `abilityLines` сделан трёхзначным (`null` = «оставить статический текст префаба», пустой = «погасить», непустой = данные), чтобы не расширять `IAbility` ради одной итерации; `EnemyCardView` — пустой сабкласс ради направления зависимости `Code.Battle.UI` → `Code.Animals.UI` → Decisions/2026-09-07-battle-hud-layout-iteration-1.md
- [2026-09-08] Боевой HUD, итерация 1 завершена частью 2 (редакторской): **девять ловушек Unity, и ни одна не даёт ни ошибки, ни варнинга** — `CanvasScaler.match` обязан быть **0 (Width)**, потому что `scaleFactor = (sw/1080)^(1-match)·(sh/1920)^match` и при `0.5` канвас на 1080×2340/1170×2532/1284×2778 выходит **978 юнитов вместо 1080** (нижний ряд перекрывался на ~41 px мёртвой зоной рейкаста), а при `1` — тем более; нижний ряд интринсически тесный (110+462+352+поля = 964 из 1080, на два зазора 116) — **ADD ANIMAL центрировать невозможно**, `x = -121`, и несущее здесь то, что `UiPulseAnimator` растит FIGHT **на 8% влево от пивота (1,0)** только в активном состоянии, то есть перекрытие проявлялось бы лишь когда кнопка загорается; если disabled-цвет красит код, у `Button` обязан стоять **`transition = None`** — иначе `ColorTint` домножается сверху (наступили дважды, получалось почти чёрное `~{0.16,0.22,0.09}`); `_notReadyColor` **считается**, а не подбирается (шейдер `UIGrayscale` даёт `lerp(luminance, rgb, 0)`, светимость тела `button_2` = 0.57, поэтому 0.55 сделал бы надпись невидимой → 0.72); штриховка на HP-баре — **`Mask` на `Fill` + Tiled-оверлей внутри**, потому что `Fill` обязан остаться `m_Type: 3` (иначе умирает `fillAmount`), Filled не тайлит, а обычный ребёнок Filled-картинки **не обрезается** по `fillAmount`; любой элемент фиксированного размера в HP-баре обязан делиться на `ownerScale` вслед за `ApplyFootprintSize()` (масштабы животных 0.35…1.0) — отсюда `_enemyIconSize`; `SafeArea` двигает не весь UI (`Level Counter` внутри `Canvas/HUD`, а `TurnOrderView` — прямой ребёнок `Canvas`), наезд на 1170×2532 **не виден без симуляции выреза**; 9-slice для `button_2.png` **геометрически невозможен** (border top≥65 + bottom≥68 = 133 > 130 px высоты) и спрайт несёт два декоративных штриха из макета — `Image.type = Simple`, арт не править; **подтверждено на живом Play Mode**: все шесть врагов стадии 2 репортят `ChickenFacade` от `PlayerAnimalFacade`, сторону берём только из `IUnitTracker` — добавлено событие `EnemyUnitRegistered` (реактивного аналога для вражеской стороны не было) → Decisions/2026-09-08-battle-hud-layout-editor-part.md
- CLAUDE.md ≤70 строк — детали в AgentsDocs/
- No Singleton — только Zenject bindings
- UniTask — новый async код только на UniTask
- Deprecated Pathfinding/ — оставлены до удаления, не трогать

## Протокол сессии
**Начало**: этот файл загружен автоматически — ничего делать не нужно.
**Конец задачи**: когда задача завершена, запусти `/update-knowledge` — скилл сам обновит этот файл.
Если пользователь говорит "готово", "задача выполнена", "закончили" → проактивно запускай `/update-knowledge`.
