 # Спецификация системы поиска пути (Pathfinding System)

**Версия:** 1.0
**Дата:** 2026-01-24
**Статус:** Черновик для обсуждения

---

## 1. Обзор

Система поиска пути (Pathfinding System) отвечает за расчет маршрутов передвижения игровых юнитов (животных и врагов) по тактическому полю боя с учетом:
- Габаритов юнита (1x1, 1x2, 2x2 клетки)
- Препятствий и занятых клеток
- Специальных типов передвижения (ходьба, полет, прыжки)
- Ограничения дальности хода

**Фокус первого этапа:** Базовое передвижение (walking) для всех юнитов. Специальные типы движения будут реализованы позже.

---

## 2. Геймплейные требования из концепт-дока

### 2.1 Игровое поле
- **Поле боя:** 8×10 клеток (ширина × высота)
- **Зона выкладки игрока:** первые 2 строки снизу (2×8 клеток)
- **Зона врагов:** верхние строки

### 2.2 Типы юнитов и их размеры

| Животное | Размер | Скорость | Особенности передвижения |
|----------|--------|----------|--------------------------|
| Слон | 2×2 | 2 клетки | Обычная ходьба |
| Олень | 1×2 | 5 клеток | Обычная ходьба |
| Гепард | 1×2 | 7 клеток | Обычная ходьба |
| Ёж | 1×1 | 1 клетка | Не атакует, только контратакует (стоит) |
| Лиса | 1×1 | 3 клетки | **Додж** - может перепрыгнуть на клетку вбок |
| 4 Курицы | 4×(1×1) | 3 клетки | Каждая занимает отдельную клетку |
| Орёл | ? | 10 клеток | **Полет** - игнорирует препятствия |
| Хамелеон | 1×1 | 2 клетки | Обычная ходьба |
| Кошка | 1×1 | 3 клетки | Обычная ходьба |
| Обезьяна | 1×1 | 2 клетки | Обычная ходьба, дальняя атака |
| Кенгуру | 1×1 | 3 клетки | **Прыжок** - может перепрыгивать через препятствия |
| Скунс | 1×1 | 2 клетки | Обычная ходьба |
| Заяц | 1×1 | 7 клеток | Обычная ходьба |

### 2.3 Типы боссов и их размеры

| Босс | Размер | Скорость | Особенности |
|------|--------|----------|-------------|
| 6 Куриц | 6×(1×1) | 10 (облепляют) | Окружают ближайшего юнита |
| Птеродактиль | 2×1 | 5 клеток | Летает туда-сюда |
| Т-Рекс | 2×2 | 2 клетки | Обычная ходьба |
| 3-4 Велоцираптора | 3-4×(1×1) | 4 клетки | Отбегают после атаки на 1-4 клетки |
| Дракон | 2×2 | - | Летает, стреляет огнем |
| Змей-Горыныч | 4×4 | - | Стоит, поливает огнем |

### 2.4 Правила передвижения

#### 2.4.1 Общие правила
- **Направления:** только 4 стороны (вверх, вниз, влево, вправо), **диагонали запрещены**
- **Очередность ходов:**
  - Первыми ходят юниты игрока (слева-направо, сверху-вниз)
  - Затем враги (слева-направо, снизу-вверх)
- **Атака:** можно атаковать по диагонали ("достаем")
- **Блокировка:** юниты блокируют клетки, нельзя пройти сквозь них

#### 2.4.2 Специальные типы передвижения

> **Примечание:** Эти механики будут реализованы на втором этапе. Сначала фокус на базовом передвижении.

**Полет (Орёл, Птеродактиль, Дракон):**
- Игнорируют других юнитов на пути
- Могут "перелететь" через препятствия
- Не могут приземлиться на занятую клетку
- Скорость: х0.3 в полёте (?)

**Додж (Лиса):**
- При попытке атаки уклоняется на клетку в сторону
- Приоритет уклонения: в сторону → назад → куда угодно свободно
- Если некуда уклониться - прыгает на месте, но всё равно доджит
- Первый раз срабатывает 100%, потом 80% у владельца, 50% у унаследовавшего
- **Не работает против AoE атаки и атаки с воздуха**

**Прыжок (Кенгуру):**
- Может перепрыгивать через препятствия
- Даёт прыжок + ×0.2 к ходу при мердже

**Отбегание (Велоцирапторы):**
- После атаки отбегают на 1-4 клетки назад
- Если не атаковали - остаются на месте

---

## 3. Выбранная система: GridPathfinding

**Решение:** Используем существующую систему GridPathfinding как базу для дальнейшего развития.

**Основные компоненты:**
```
GridPathfinding/
├── IPathfindingService.cs      - Интерфейс сервиса поиска пути
├── PathfindingService.cs       - Реализация A* алгоритма
├── IGridManager.cs             - Интерфейс управления игровой сеткой
├── GridManager.cs              - Реализация сетки (хранение состояния)
├── GridCell.cs                 - Узел сетки (данные + состояние для A*)
├── UnitSize.cs                 - Размер юнита (Width × Height)
├── Direction.cs                - Направление юнита (North/East/South/West)
├── PathRequest.cs              - Value object - запрос на поиск пути
└── PathResult.cs               - Value object - результат поиска
```

**Преимущества:**
- ✅ Использует интерфейсы (IPathfindingService, IGridManager) - готово для Zenject DI
- ✅ Object pooling для производительности (_openList, _closedSet, _neighbors)
- ✅ Учитывает габариты юнита (UnitSize: 1x1, 1x2, 2x2)
- ✅ Учитывает поворот юнита (Direction) для несимметричных форм
- ✅ Четкое разделение ответственности (Single Responsibility Principle)
- ✅ PathRequest/PathResult - удобные value objects
- ✅ Готовая рабочая реализация A* с Manhattan distance
- ✅ Метод CanPlaceUnit для проверки размещения юнита

**Что нужно доработать:**
- ⚠️ Добавить учет максимальной дальности хода (maxDistance)
- ⚠️ Реализовать GetReachableCells() для получения всех доступных клеток
- ⚠️ Улучшить визуализацию для отладки (Gizmos)
- ⚠️ Добавить поддержку специальных типов передвижения (в будущем: полет, прыжок)

**Другие системы в проекте:**
- `NewPathfinding/` - альтернативная реализация, не используется (Singleton, статические классы)

---

## 4. Требования к новой системе

### 4.1 Архитектурные требования (из CLAUDE.md)

**Обязательные:**
- ✅ Dependency Injection через Zenject
- ✅ Интерфейсы для всех зависимостей
- ✅ Применение SOLID принципов
- ✅ Clean Architecture layers
- ✅ Async/await вместо корутин (для анимации движения)
- ✅ Object pooling где необходимо
- ✅ Кеширование компонентов

**Запрещенные паттерны:**
- ❌ Singleton паттерн
- ❌ Статические классы для логики
- ❌ FindObjectOfType, GameObject.Find в runtime

### 4.2 Функциональные требования

#### 4.2.1 Основные возможности

1. **Поиск пути с учетом габаритов:**
   - Юнит размером 2×2 не должен пытаться пройти через проход 1 клетка
   - Anchor point юнита (точка отсчета) = левый нижний угол

2. **Типы передвижения:**
   - `Walking` - обычная ходьба (блокируется другими юнитами)
   - `Flying` - полет (игнорирует юнитов на пути, но не может приземлиться на занятую клетку)
   - `Jumping` - прыжок (может перепрыгивать через препятствия)
   - `Teleport` - телепортация (мгновенное перемещение, для скиллов)

3. **Ограничение дальности:**
   - Учет максимальной дальности хода юнита
   - Возможность получить все доступные клетки в радиусе хода

4. **Направление юнита:**
   - Поворот юнита влияет на занимаемые клетки (для несимметричных юнитов 1×2)
   - 4 направления: North, East, South, West

5. **Проверка возможности размещения:**
   - Можно ли разместить юнита размером NxM на позиции (x, y)?
   - Учет поворота

6. **Визуализация (опционально):**
   - Подсветка доступных для хода клеток
   - Визуализация найденного пути
   - Цветовое кодирование (свободно/занято/недоступно)

#### 4.2.2 Дополнительные возможности

7. **Поиск ближайшей свободной клетки:**
   - Если целевая клетка занята, найти ближайшую свободную

8. **Группировка юнитов (для куриц-боссов):**
   - "Облепление" ближайшего врага со всех сторон

9. **Кеширование путей:**
   - Для оптимизации, если несколько юнитов идут в одну точку

### 4.3 Производительность

**Целевые метрики:**
- Поиск пути для одного юнита: **< 5 мс**
- Расчет всех доступных клеток: **< 10 мс**
- Без аллокаций в куче (Object pooling)
- Поддержка до **20 юнитов** одновременно на поле

---

## 5. Предлагаемая архитектура

### 5.1 Слои Clean Architecture

```
Presentation Layer (UI, Visualizers)
    ↓
Application Layer (PathfindingService, MovementController)
    ↓
Domain Layer (Grid, PathFinder, Interfaces)
    ↓
Infrastructure Layer (GridData, Serialization)
```

### 5.2 Основные интерфейсы

#### IPathfindingService
```csharp
public interface IPathfindingService
{
    // Поиск пути с полными параметрами
    PathResult FindPath(PathRequest request);

    // Получить все доступные клетки в радиусе хода
    List<Vector2Int> GetReachableCells(
        Vector2Int startPosition,
        int maxDistance,
        UnitSize unitSize,
        MovementType movementType);

    // Проверка возможности размещения
    bool CanPlaceUnit(
        Vector2Int position,
        UnitSize size,
        Direction direction,
        bool ignoreOccupied = false);
}
```

#### IGridManager
```csharp
public interface IGridManager
{
    int Width { get; }
    int Height { get; }

    GridCell GetCell(int x, int y);
    GridCell GetCell(Vector2Int position);

    bool IsInBounds(int x, int y);
    bool IsWalkable(int x, int y);

    void SetOccupied(Vector2Int position, UnitSize size, object occupant);
    void SetFree(Vector2Int position, UnitSize size);

    Vector3 GetWorldPosition(int x, int y);
    Vector2Int GetGridPosition(Vector3 worldPosition);
}
```

#### IMovementController
```csharp
public interface IMovementController
{
    // Переместить юнита по пути с анимацией
    Task MoveAlongPathAsync(
        IUnit unit,
        List<Vector2Int> path,
        CancellationToken token);

    // Проверка возможности хода
    bool CanMove(IUnit unit, Vector2Int targetPosition);
}
```

### 5.3 Value Objects

#### MovementType (новый)
```csharp
public enum MovementType
{
    Walking,    // Обычная ходьба (блокируется юнитами)
    Flying,     // Полет (игнорирует юнитов на пути)
    Jumping,    // Прыжок (может перепрыгивать)
    Teleport    // Телепортация (мгновенно)
}
```

#### PathRequest (расширенный)
```csharp
public class PathRequest
{
    public Vector2Int StartPosition { get; }
    public Vector2Int TargetPosition { get; }
    public UnitSize UnitSize { get; }
    public Direction Direction { get; }
    public MovementType MovementType { get; }  // НОВОЕ
    public int MaxDistance { get; }            // НОВОЕ
    public bool IgnoreOccupied { get; }
    public object RequestingUnit { get; }
}
```

---

## 6. Алгоритмы

### 6.1 A* Pathfinding

**Алгоритм:** A* (A-star) с Manhattan distance heuristic

**Основные шаги:**
1. Инициализация:
   - Open list (приоритетная очередь по FCost)
   - Closed set (уже обработанные узлы)

2. Главный цикл:
   - Взять узел с минимальным FCost из Open list
   - Если это целевой узел → путь найден
   - Добавить в Closed set
   - Для каждого соседа:
     - Пропустить, если в Closed set
     - **Проверить, может ли юнит занять эту клетку с учетом размера**
     - Рассчитать новый GCost
     - Если лучше текущего → обновить и добавить в Open list

3. Эвристика (HCost):
   ```
   H = |targetX - currentX| + |targetY - currentY|
   ```

4. Финальная стоимость:
   ```
   FCost = GCost + HCost
   ```

### 6.2 Проверка размещения (CanPlaceUnit)

**Алгоритм:**
```
Для каждой клетки, которую займет юнит:
    Рассчитать координаты с учетом направления
    Проверить:
        - В границах сетки?
        - Проходима? (IsWalkable)
        - Свободна? (не занята другим юнитом)
    Если хоть одна не подходит → return false
return true
```

**Пример для юнита 1×2 (олень):**
- Direction = North: занимает клетки (x, y) и (x, y+1)
- Direction = East: занимает клетки (x, y) и (x+1, y)

### 6.3 Получение доступных клеток (GetReachableCells)

**Алгоритм:** BFS (Breadth-First Search) с ограничением по расстоянию

```
queue = [startPosition]
visited = {startPosition}
result = []

while queue not empty:
    current = queue.pop()
    distance = CalculateDistance(start, current)

    if distance > maxDistance:
        continue

    if CanPlaceUnit(current, unitSize, direction):
        result.add(current)

    for neighbor in GetNeighbors(current):
        if neighbor not in visited:
            visited.add(neighbor)
            queue.add(neighbor)

return result
```

---

## 7. Особые случаи

### 7.1 Полет (Flying)

**Модификация алгоритма:**
- При проверке соседей игнорировать занятость промежуточных клеток
- Финальная клетка должна быть свободна
- **Вопрос:** Можно ли лететь над юнитом 2×2? (скорее всего да)

### 7.2 Додж (Dodge)

**Это не pathfinding!** Додж - это реакция на атаку:
- Срабатывает в момент атаки
- Ищет свободную клетку в приоритете: сторона → назад → любая
- Это логика боевой системы, а не pathfinding

### 7.3 Прыжок (Jumping)

**Модификация алгоритма:**
- Может "перепрыгнуть" через 1 занятую клетку
- Финальная клетка должна быть свободна
- **Вопрос:** Максимальная дальность прыжка?

### 7.4 Облепление (Куриц-боссы)

**Алгоритм:**
1. Найти ближайшего юнита игрока
2. Для каждой курицы найти ближайшую свободную клетку вокруг цели
3. Рассчитать пути для всех куриц одновременно
4. Резервировать клетки, чтобы курицы не шли в одну точку

---

## 8. Визуализация (опционально)

### 8.1 Отладочная визуализация

**Цели:**
- Помочь геймдизайнеру настраивать баланс
- Отладка проблем с pathfinding

**Возможности:**
- Toggle визуализации сетки (Gizmos)
- Подсветка доступных для хода клеток (зеленый)
- Подсветка занятых клеток (красный)
- Отрисовка найденного пути (синяя линия)
- Визуализация зоны атаки

### 8.2 Игровая визуализация

**Для игрока:**
- Подсветка клеток, куда можно переместить выбранного юнита
- Анимация движения по пути

---

## 9. Интеграция с другими системами

### 9.1 Battle System
- **MovementController** использует Pathfinding для расчета хода
- После нахождения пути анимирует движение юнита
- Обновляет занятость клеток в GridManager

### 9.2 Merge System
- Использует `CanPlaceUnit` для проверки возможности размещения смердженного юнита

### 9.3 Animation System
- **MovementController** работает с Animator для проигрывания анимации ходьбы/полета

---

## 10. Вопросы для обсуждения

### 10.1 Геймплейные вопросы

1. ✅ **РЕШЕНО: Направление (Direction) для юнитов**
   - **Решение:** Direction учитывается только для прямоугольных юнитов (1×2, 2×1)
   - Для квадратных юнитов (1×1, 2×2) поворот не важен - они симметричны
   - Это упрощает логику и улучшает производительность

2. ⏳ **ОТЛОЖЕНО: Размер орла**
   - Концепт-док не указывает
   - Решение отложено на будущее
   - Временно можно использовать 1×1 для прототипирования

### 10.2 Вопросы на будущее (специальные типы передвижения)

3. **Полет через юнитов 2×2?**
   - Можно ли лететь над слоном?
   - **Предложение:** да, можно

4. **Максимальная дальность прыжка кенгуру?**
   - Может перепрыгнуть 1 клетку? Или больше?
   - **Предложение:** 1 клетка (перепрыгивает соседнего юнита)

5. **Скорость орла "×0.3 в полёте"?**
   - Что это значит? Орел медленнее летает?
   - **Предложение:** Это про анимацию (медленнее двигается визуально)

6. **Велоцирапторы отбегают "на 1-4 клетки"?**
   - Рандомно от 1 до 4?
   - **Предложение:** Рандомно, зависит от оставшегося HP или просто случайно

---

## 11. План реализации

### Этап 1: Базовый функционал (приоритет)
**Цель:** Работающий pathfinding для обычного передвижения

- [ ] Проверить работу GridPathfinding с Zenject
- [ ] Добавить учет максимальной дальности хода (maxDistance в PathRequest)
- [ ] Реализовать GetReachableCells() - получение всех доступных клеток в радиусе
- [ ] Добавить Gizmos визуализацию для отладки сетки
- [ ] Написать unit-тесты для базовых сценариев
- [ ] Протестировать с разными размерами юнитов (1×1, 1×2, 2×2)

### Этап 2: Интеграция с боевой системой
**Цель:** Юниты могут двигаться во время боя

- [ ] Создать IMovementController интерфейс
- [ ] Реализовать MovementController с анимацией движения
- [ ] Интегрировать с Battle System
- [ ] Обновлять занятость клеток в GridManager после хода

### Этап 3: Дополнительные возможности (опционально)
**Позже, при необходимости:**

- [ ] Специальные типы передвижения (Flying, Jumping)
- [ ] Облепление для куриц-боссов
- [ ] UI подсветка доступных клеток для игрока
- [ ] Оптимизация производительности (если нужно)

---

## 12. Метрики успеха

**Функциональные:**
- ✅ Все юниты корректно находят путь с учетом габаритов
- ✅ Летающие юниты игнорируют препятствия
- ✅ Нет визуальных багов (юниты не застревают, не проходят сквозь стены)

**Технические:**
- ✅ Поиск пути < 5 мс
- ✅ Нет аллокаций в куче
- ✅ 100% покрытие unit-тестами для PathfindingService
- ✅ Следование SOLID и Clean Architecture

**Игровые:**
- ✅ Геймдизайнер может легко настроить размер поля
- ✅ Легко добавлять новые типы юнитов
- ✅ Удобная визуализация для отладки баланса

---

## 13. Риски

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Производительность на больших полях | Средняя | Высокое | Оптимизация A*, тесты производительности |
| Сложность облепления куриц | Высокая | Среднее | Реализовать в последнюю очередь, упростить если нужно |
| Конфликты с анимацией | Средняя | Среднее | Четкий интерфейс между Pathfinding и Animation |
| Баги с габаритами 2×2 | Средняя | Высокое | Тщательное тестирование, визуализация |

---

## 14. Приложения

### 14.1 Примеры использования

**Пример 1: Простой поиск пути**
```csharp
var request = new PathRequest(
    startPosition: new Vector2Int(0, 0),
    targetPosition: new Vector2Int(5, 5),
    unitSize: UnitSize.Small,
    direction: Direction.North,
    movementType: MovementType.Walking,
    maxDistance: 10
);

PathResult result = _pathfindingService.FindPath(request);

if (result.Success)
{
    await _movementController.MoveAlongPathAsync(unit, result.Path, token);
}
```

**Пример 2: Получение доступных клеток**
```csharp
List<Vector2Int> reachable = _pathfindingService.GetReachableCells(
    startPosition: unit.Position,
    maxDistance: unit.MovementRange,
    unitSize: unit.Size,
    movementType: unit.MovementType
);

_visualizer.HighlightCells(reachable, Color.green);
```

### 14.2 Диаграммы

#### Схема взаимодействия компонентов
```
[BattleController]
        ↓
[MovementController] ←→ [IPathfindingService]
        ↓                        ↓
[AnimationController]    [IGridManager]
```

#### Поток данных при поиске пути
```
User Input → BattleController
          → MovementController.MoveUnit()
          → PathfindingService.FindPath()
          → GridManager.GetCell() (много раз)
          → PathResult
          → MovementController.MoveAlongPathAsync()
          → AnimationController.PlayWalkAnimation()
```

---

## 15. Следующие шаги

1. **Обсудить геймплейные вопросы** - ответить на вопросы из раздела 10.1
2. **Согласовать архитектуру** - одобрить предложенные интерфейсы (раздел 5.2)
3. **Начать реализацию** - по плану из раздела 11, Этап 1
4. **Удалить NewPathfinding** - чтобы избежать путаницы, оставить только GridPathfinding

---

**Конец документа**
